# MotoMate 项目问题与解决方案档案

创建日期：2026-08-29  
维护范围：只沉淀项目中已经出现的问题、判断过程、解决方案、验证结果和可迁移经验。本文不推进功能开发，不替代 PRD、长期记忆、知识库或规则引擎。  
证据原则：仓库文件可作为项目证据，但本文严格区分“已确认事实、当时假设、建议、后续结果”。没有证据的内容标记为“待验证”。

## 0. 使用说明

- 状态定义：`已解决`表示该问题在当前阶段已有明确决策与验证；`部分解决`表示策略或局部实现已完成，但仍缺上线、用户验证或后续工程闭环；`待验证`表示已有方向但证据不足；`开放`表示尚未形成可执行方案。
- 复核表述：Codex 对资料、异常和规则的检查统一称为 `Codex复核` 或 `Codex operational review`，不得写成外部专家审核或 Ethan 逐字段人工核验。
- 推荐边界：`eligible` 只表示进入确定性规则筛选池；只有 `rule_pool_eligibility.status=eligible` 且 `recommendation_review_status.status=approved` 才能进入最终推荐池。
- 安全边界：本文不记录真实密钥、验证码、私人邮箱、Token、账号迁移细节或其他敏感凭据。

## 1. 案例总览

| 编号 | 案例 | 状态 | 主要证据 |
|---|---|---|---|
| MPS-001 | MVP 从泛摩托咨询收敛为新手选车推荐 + 轻量二手助手 | 部分解决 | `MVP产品需求文档.md`、`项目长期记忆.md`、`README.md` |
| MPS-002 | 访谈样本少，限制结论边界 | 已解决 | `访谈记录整理.md`、`MVP产品需求文档.md`、`项目长期记忆.md` |
| MPS-003 | 知识库从模型抽取草案走向三正交状态与准入规则 | 已解决 | `知识库状态与准入规则V0.2.md`、`knowledge_base_outputs/知识库状态重构V0.2报告.md` |
| MPS-004 | 智谱 A/B/C 后默认文本抽取推荐 glm-4.7 | 已解决 | `knowledge_base_outputs/GLM45Air_GLM46V_GLM47_同输入ABC基准报告.md` |
| MPS-005 | 价格来源不一致与 cache-first / stale-while-revalidate 策略 | 部分解决 | `知识库状态与准入规则V0.2.md`、`项目长期记忆.md` |
| MPS-006 | 第一批 8 配置异常处理：AMT、PCX160 等 | 已解决 | `knowledge_base_outputs/第一批8配置端到端收口报告.md`、校验脚本 |
| MPS-007 | `eligible` 不等于 `recommendation approved` | 已解决 | `知识库状态与准入规则V0.2.md`、`rules-engine.js`、校验结果 |
| MPS-008 | 语义增强区分事实、确定性推导和产品假设 | 部分解决 | `knowledge_base_outputs/semantic_enrichment/语义字段V0.1说明.md` |
| MPS-009 | 采用规则筛选 + 低权重语义排序 + AI 解释 | 部分解决 | `MVP产品需求文档.md`、`rules-engine.js`、`system_prompt.md` |
| MPS-010 | 网页原型接入曾被判断过早，改为 Agent 需求与架构前置 | 部分解决 | `项目长期记忆.md`、`MVP产品需求文档.md` |
| MPS-011 | 不盲目建设多 Agent，采用编排器 + 模块化工具 | 部分解决 | `MVP产品需求文档.md`、`项目长期记忆.md` |
| MPS-012 | GitHub、私有仓库、密钥排除与版本管理安全实践 | 部分解决 | `.gitignore`、`.env.example`、`README.md`、Git 状态 |
| MPS-013 | 智谱权益剩余窗口、资源额度与高 Token 任务使用边界 | 部分解决 | 主任务同步的账户反馈与截图摘要、既有模型基准报告 |
| MPS-014 | 通过 11 阶段确认形成设计冻结点，并进入 8 配置内部 Alpha | 部分解决 | `Agent需求与架构设计V0.1.md`、主项目合并核查 |
| MPS-015 | 库外车型联网发现必须经过双源证据与预算门禁 | 部分解决 | `external-evidence-pipeline.js`、`external-candidate-discovery.js`、真实网页试跑 |
| MPS-016 | 50 条黄金评测发现落地预算仍按裸车预算筛选 | 已解决 | `evaluations/golden/`、`scripts/run-golden-evals.mjs`、预算回归测试 |
| MPS-017 | 正式推荐批准批次与外部邀请 Alpha 发布边界 | 部分解决 | 批准 Schema、`scripts/check-alpha-test-readiness.mjs`、17 配置 MVP 池 |
| MPS-018 | 跨任务只回复“好的”但没有执行，不能把消息送达当作完成 | 部分解决 | Codex 任务协作记录、共享工作区 Git 状态 |
| MPS-019 | Responses continuation 出现缺少 `call_id` 的协议错误 | 部分解决 | 本地中转站报错与重启验证 |
| MPS-020 | 历史结构化需求覆盖当前开放问题，导致重复推荐 | 已解决 | `alpha-orchestrator.js`、API 与黄金回归 |
| MPS-021 | 短期与长期记忆政策、实现和隐私告知相互冲突 | 已解决 | `server/memory-store.js`、PRD、架构、测试手册 |
| MPS-022 | 年轻化回复降低 AI 味时放松了新手安全边界 | 已解决 | `open-answer-agent.js`、真实模型试跑、Agent 评测 |

---

## MPS-001：MVP 从泛摩托咨询收敛为“新手选车推荐 + 轻量二手助手”

**案例编号、日期/阶段、状态**
MPS-001；2026-08-27 至 2026-08-30；部分解决。产品范围与叙事已确认，首轮二手能力边界已收窄，真实用户验证尚未完成。

**问题现象与用户/业务影响**  
早期方向容易落成泛摩托咨询：新车、二手、维修、保养、改装、交易都能问，但每条都不够可信。对面试作品来说，这会削弱产品主线；对真实用户来说，最关键的“我该买什么车”也会被淹没。

**发现问题的证据**  
`MVP产品需求文档.md` 明确写入项目第一目标、核心流程、辅助能力、首轮验证规模和“不做”的范围。`项目长期记忆.md` 记录“新手选车推荐是核心流程，轻量二手车助手是辅助亮点”。`README.md` 的当前范围也把核心流程写为新手选车推荐，辅助能力写为轻量二手车风险助手。2026-08-30 主任务同步的最新事实进一步确认：首轮二手能力只做文字助手，图片能力推迟至首轮 10-20 人测试后再依据真实需求决定是否建设；当前 PRD 中图片首发设想已经过期，但原 PRD 尚未同步更新。

**根因分析**  
已确认根因是 MVP 目标同时承担“面试作品”和“可上线验证”，如果不先收敛用户问题，会把有限时间消耗在交易、维修、改装等高风险场景。二手助手保留为辅助亮点，是因为二手车源判断能体现风险边界和信息缺口整理，但不应与核心推荐同等权重。

**当时考虑过的方案与取舍**  
方案 A 是泛摩托咨询，覆盖广但可信度和验收成本高。方案 B 是直接做二手交易平台，商业想象大但履约、风控、检测和车商链路过重。方案 C 是新手选车推荐为主、轻量二手助手为辅，能优先验证决策价值，并把交易责任边界控制住。当前选择方案 C。

**最终决策及为什么选择**  
决策是：MVP 主叙事为“新手选车推荐”，二手助手在首轮先收窄为文字助手，只做信息缺口、风险提示、卖家问题和线下验车清单。图片能力不进入首轮实现，等 10-20 人测试后根据真实需求再判断。原因是这条路径最能同时满足用户价值、上线可行性和 AI 产品经理面试叙事，也避免在隐私、脱敏、视觉模型和图片评测链路尚未闭环前扩大风险。

**实施动作或关联产物**  
关联产物包括 `MVP产品需求文档.md`、`system_prompt.md`、`README.md`、`rules-engine.js` 和首批规则池。PRD 中明确排除维修、保养、改装、交易撮合、支付担保和事故/泡水/调表鉴定。注意：PRD 当前仍含图片首发相关描述，已与最新首轮二手文字助手策略不一致，后续需由主任务同步修订 PRD 与 system prompt。

**验证方法与结果**  
当前完成的是文档、规则边界和阶段策略验证，不是用户成功验证。PRD 设定 10 至 20 位首轮用户、前 5 位深度可用性测试、70% 初步成功门槛，但仓库内没有证据显示该测试已执行。当前知识库覆盖仍只有 8 个配置可用于 `development_preview` 链路验证，`recommendation approved` 仍为 0。

**尚存风险、后续触发条件**  
风险是用户真实需求可能更偏二手车源、图片识别或维修保养。触发条件是首轮测试中高频出现非选车需求、二手图片诉求显著强于文字助手、二手助手使用率明显高于新手推荐，或推荐帮助度低于 70% 门槛。

**可迁移的 AI 产品经理方法**  
先定义核心用户任务，再定义 AI 能做什么。MVP 不应按模型能力或原型能力扩张范围，而应按“用户关键决策 + 可验证数据 + 可控风险”收敛场景；已经写进 PRD 的能力也可以在新证据出现后降级或后移。

**面试表达版本**  
30 秒摘要：我把项目从泛摩托问答收敛为新手选车推荐，因为这是用户最强的决策痛点，也最适合用结构化数据和 AI 解释形成可信闭环。二手助手首轮进一步收窄为文字助手，图片能力等真实测试后再决定，避免为了显得完整提前承担隐私和视觉误判风险。  
追问可展开：可以讲为什么不做维修/交易、为什么二手不做最终鉴定、为什么图片能力后移、为什么首轮用帮助度和具体帮助标签衡量成功。

**相关文件**  
`MVP产品需求文档.md`；`项目长期记忆.md`；`README.md`；`system_prompt.md`。

---

## MPS-002：访谈样本少，如何限制结论边界

**案例编号、日期/阶段、状态**  
MPS-002；2026-08-27 至 2026-08-29；已解决。调研结论边界已在 PRD 与长期记忆中明确。

**问题现象与用户/业务影响**  
仓库中的访谈只有 6 条有效记录，且部分字段为空。如果直接把这些材料解释成市场统计，会导致品牌覆盖、用户偏好和功能优先级被小样本误导。

**发现问题的证据**  
`访谈记录整理.md` 标注来源为飞书多维表格访谈记录，共 6 条有效记录。记录中价格被多次提到，出现车型包括灰石 250、500SR、450CLC 等，但回答不完整。`MVP产品需求文档.md` 和 `项目长期记忆.md` 均提醒：该材料只能支持方向判断，不能推断整体市场品牌偏好。

**根因分析**  
已确认根因是样本量小、招募来源和回答完整性不足。访谈更适合作为早期问题发现，不适合作为统计证明。

**当时考虑过的方案与取舍**  
一种做法是用 6 条访谈直接确定品牌清单和功能优先级，速度快但风险高。另一种做法是只提取高频问题线索，并在产品内继续收集反馈。当前选择第二种。

**最终决策及为什么选择**  
决策是：访谈支持“价格透明、真实体验、品控与信息可信是重点”，但不单独证明市场偏好、品牌覆盖率或完整车型清单。车型覆盖仍以价格范围、在售状态和资料可核验为准。

**实施动作或关联产物**  
PRD 设定首轮 10 至 20 位用户验证，并要求熟人与陌生样本分开统计。推荐反馈要求同时收集 5 分帮助度评分、具体帮助标签和简短说明。

**验证方法与结果**  
已验证的是文档口径：PRD 和长期记忆均包含小样本限制。尚未验证的是真实用户测试结果，因为仓库中没有首轮用户反馈数据。

**尚存风险、后续触发条件**  
风险是当前需求排序仍可能受早期样本偏差影响。触发条件是产品内反馈显示高频需求与早期访谈不一致，或陌生用户反馈显著弱于熟人样本。

**可迁移的 AI 产品经理方法**  
小样本访谈应输出“问题假设”和“验证计划”，不要输出市场定论。面试中要主动说明样本边界，比强行包装数据更可信。

**面试表达版本**  
30 秒摘要：我没有把 6 条访谈包装成市场结论，而是把它们当成问题发现材料。它们支持价格透明和信息可信的重要性，但品牌偏好和市场覆盖要靠后续测试验证。  
追问可展开：可以讲熟人与陌生样本分层、单用户成功口径、70% 阶段门槛，以及为什么高分还必须有“具体帮助”说明。

**相关文件**  
`访谈记录整理.md`；`MVP产品需求文档.md`；`项目长期记忆.md`。

---

## MPS-003：知识库从模型抽取草案走向三正交状态、准入校验和正式规则池

**案例编号、日期/阶段、状态**  
MPS-003；2026-08-28；已解决。V0.2 状态规则、目录语义和校验脚本已建立。

**问题现象与用户/业务影响**  
早期知识库中容易把模型抽取、字段核验、规则池准入和推荐批准混在一个状态里。这样会让“字段看起来完整”被误读为“可以推荐给用户”，产生错误推荐风险。

**发现问题的证据**  
`知识库状态与准入规则V0.2.md` 明确用 `fact_verification_status`、`rule_pool_eligibility`、`recommendation_review_status` 三个正交维度替代旧的混合状态。`knowledge_base_outputs/知识库状态重构V0.2报告.md` 记录旧目录保留、非破坏迁移、状态 manifest、10% 抽样和校验器。

**根因分析**  
已确认根因是旧状态把“事实证据”“规则可筛选”“推荐可对外输出”混成一条链路，任何一步通过都可能被误升级。

**当时考虑过的方案与取舍**  
方案 A 是继续使用 `field_verified / pending_verification / recommendation_ready`，改动小但语义混乱。方案 B 是彻底拆成三个维度，改动更多但可审计。当前选择方案 B。

**最终决策及为什么选择**  
决策是建立 V0.2：事实字段只回答证据等级，规则池只回答能否参加确定性筛选，推荐复核只回答能否进入最终推荐池。这样可以允许“数据足够参与规则”与“尚未推荐批准”并存。

**实施动作或关联产物**  
关联产物包括 `知识库状态与准入规则V0.2.md`、`knowledge_base_outputs/legacy_manifest/status_directory_mapping.json`、`knowledge_base_outputs/exceptions/current_status_remap.json`、`scripts/validate-kb-status-v02.mjs` 和 `scripts/select-kb-review-sample.mjs`。

**验证方法与结果**  
V0.2 报告记录状态 manifest 8 条候选、枚举和交叉约束通过，自动推荐升级 0，自动 eligible 升级 0，抽样总体 20 条、样本 2 条。本文创建时再次运行 `scripts/validate-eligible-pool-v02.mjs`，结果为 8 条有效、eligible 8、approved 0、错误列表为空。

**尚存风险、后续触发条件**  
风险是后续新增数据时又绕过 V0.2 状态。触发条件是出现新目录、新字段或模型产物直接进入推荐链路。

**可迁移的 AI 产品经理方法**  
AI 产品的数据状态要拆清楚：模型产物、事实核验、规则准入、产品批准不能合并。状态机不是技术洁癖，而是防止错误推荐的产品安全机制。

**面试表达版本**  
30 秒摘要：我把知识库状态拆成事实核验、规则池资格和推荐复核三个正交维度，解决了“字段完整就误以为可推荐”的风险。模型输出永远先是草稿，必须经过来源和规则门禁。  
追问可展开：可以讲为什么旧目录保留但不按目录推断状态，以及为什么 Codex 复核不能写成人工专家审核。

**相关文件**  
`知识库状态与准入规则V0.2.md`；`knowledge_base_outputs/知识库状态重构V0.2报告.md`；`scripts/validate-kb-status-v02.mjs`；`knowledge_base_outputs/exceptions/current_status_remap.json`。

---

## MPS-004：智谱模型 A/B/C 基准后，为什么文本抽取推荐 glm-4.7

**案例编号、日期/阶段、状态**  
MPS-004；2026-08-28；已解决。默认文本抽取候选已建议为 `glm-4.7`，但未永久修改 `.env`。

**问题现象与用户/业务影响**  
公开车型资料抽取需要稳定输出固定 JSON、保持配置绑定、遵守来源白名单，并控制 Token 和延迟。早期 `glm-5.3` 在批量 JSON 任务上出现输出预算被思考 token 吞掉的问题；`glm-4.5-air` 虽便宜但存在配置拆分不稳定。

**发现问题的证据**  
`knowledge_base_outputs/V0.1_UHR150_250SR_试采报告.md` 记录 `glm-5.3` 两次 JSON 不可解析。`knowledge_base_outputs/GLM45Air_UHR150_模型路由验收报告.md` 记录 `glm-4.5-air` 可解析但多配置拆分不稳定。`knowledge_base_outputs/GLM45Air_GLM46V_GLM47_同输入ABC基准报告.md` 记录同输入 A/B/C：`glm-4.5-air` 2/3 通过且重试仍失败，`glm-4.6v` 和 `glm-4.7` 均 3/3 通过；`glm-4.7` Token 更少、延迟更低。

**根因分析**  
已确认根因是不同模型在“固定 Schema 单配置抽取”任务上的约束遵循能力不同。`glm-4.5-air` 的问题集中在配置名绑定，`glm-5.3` 的问题集中在强制思考导致 JSON 输出失败。

**当时考虑过的方案与取舍**  
继续默认 `glm-4.5-air` 的优势是额度和成本，但重复失败会增加重试成本和人工复核。使用 `glm-4.6v` 可行，但本轮纯文本并未使用视觉能力，且 Token 和延迟不如 `glm-4.7`。使用 `glm-4.7` 在当前任务上结构正确率与 `glm-4.6v` 相同，效率更好。

**最终决策及为什么选择**  
决策是：公开文本、固定 Schema、单配置抽取默认候选建议为 `glm-4.7`；视觉/截图抽取暂定 `glm-4.6v`，但必须另做视觉基准。失败降级为 `glm-4.7 -> glm-4.6v -> glm-4.5-air -> 人工复核`。

**实施动作或关联产物**  
产物包括 A/B/C 基准报告、路由验证 JSON 和 extraction draft。未永久修改 `.env`，未把模型结果直接升级为事实。

**验证方法与结果**  
A/B/C 首次 9 次均 HTTP 200、JSON 可解析、URL 白名单通过、无来源补全 0。`glm-4.7` 三样本 3/3 通过，合计 3367 tokens，平均延迟 11.81 秒。P0 扩建后滚动单配置首次通过率为 10/12，即 83.3%，仍需保留配置绑定门禁。

**尚存风险、后续触发条件**  
风险是小样本基准不能外推长期稳定率，且 `glm-4.7` 仍在 AMT、灰石 CVT 等配置名场景失败过。触发条件是新增车型配置名复杂、来源文本混杂或滚动通过率下降。

**可迁移的 AI 产品经理方法**  
模型选择不能只看“更强”或“更便宜”，要按任务定义基准：结构正确率、事实约束、绑定稳定性、延迟、Token、失败重试和人工复核成本。

**面试表达版本**  
30 秒摘要：我做了同输入 A/B/C 基准，发现 glm-4.7 在文本单配置抽取上和 glm-4.6v 一样 3/3 通过，但更省 Token、延迟更低；glm-4.5-air 重复在配置绑定上失败。所以默认文本抽取推荐 glm-4.7，但模型输出仍只是草稿。  
追问可展开：可以讲为何不因账户额度选择模型，为什么保留配置绑定门禁，以及为什么视觉模型需另测。

**相关文件**  
`knowledge_base_outputs/GLM45Air_GLM46V_GLM47_同输入ABC基准报告.md`；`knowledge_base_outputs/GLM45Air_UHR150_模型路由验收报告.md`；`knowledge_base_outputs/V0.1_P0五检索单元扩建批次报告.md`。

---

## MPS-005：价格来源不一致、价格口径与 cache-first / stale-while-revalidate

**案例编号、日期/阶段、状态**  
MPS-005；2026-08-28 至 2026-08-29；部分解决。策略已确认，工程实现尚未开始。

**问题现象与用户/业务影响**  
摩托车价格可能有官网公开价、平台参考价、经销商报价、优惠价、二手挂牌价等多种口径。如果直接比较或覆盖，会导致预算筛选错误，甚至把低价平台观察值当作官方裸车价推荐。

**发现问题的证据**  
`MVP产品需求文档.md` 要求区分官方指导价、建议零售价、经销商报价、公开优惠价、二手挂牌价和成交价。`知识库状态与准入规则V0.2.md` 的价格策略 A 规定官网公开价写入 `official_public_price_cny`，合格平台价写入 `qualified_platform_reference_price_cny`，预算准入用 `budget_guard_price_cny=max(...)`。`项目长期记忆.md` 记录 Ethan 已确认按需刷新策略，但尚未实现。

**根因分析**  
已确认根因是价格来源口径不同，且有效期不同。官网公开价格、平台参考价和门店实际成交价不能互相覆盖；平台价格低于官网价也不一定是事实冲突，可能只是市场优惠或配置口径不清。

**当时考虑过的方案与取舍**  
一种做法是只用官网价，最稳但可能不够贴近市场。另一种是取最低平台价，体验上看起来便宜但风险最大。当前方案是官网价作为正式基础，平台价只有同版本、同配置、同口径、24 小时内核验才参与预算保护价。

**最终决策及为什么选择**  
决策是采用价格策略 A，并在未来询价链路中使用 `cache-first / stale-while-revalidate`：缓存有效直接返回；缓存过期展示旧值和最后更新时间，同时后台刷新；无缓存才展示核验进度。失败或超时保留旧值并标记 `stale`，禁止用模型记忆补价。

**实施动作或关联产物**  
策略写入 `知识库状态与准入规则V0.2.md` 和 `项目长期记忆.md`。第一批 8 配置收口时，本批平台价均未满足合格条件，因此 `qualified_platform_reference_price_cny=null`，`budget_guard_price_cny` 使用官网公开价。

**验证方法与结果**  
已验证的是规则池价格字段通过 `scripts/validate-eligible-pool-v02.mjs`：8 条有效、eligible 8、错误 0。尚未验证的是按需刷新服务，因为实现尚未开始。

**尚存风险、后续触发条件**  
风险是上线后价格缓存过期、网页抓取失败或站点反爬导致核验慢。触发条件是用户询价、价格超过有效期、平台价与官网价同口径冲突，或后台出现价格错误反馈。

**可迁移的 AI 产品经理方法**  
价格不是一个数字，而是“数值 + 来源 + 口径 + 配置匹配 + 时间”。涉及用户预算的字段必须以状态和历史记录管理，不能交给模型即兴解释。

**面试表达版本**  
30 秒摘要：我把价格拆成官网公开价、合格平台参考价和展示性市场观察值，预算筛选使用保护价而不是最低价。缓存过期时不阻塞用户看旧值，但后台刷新并标 stale，避免模型凭记忆补价。  
追问可展开：可以讲同口径冲突才阻塞、二手挂牌价不能写成成交价、以及为什么 stale-while-revalidate 不降低准入门槛。

**相关文件**  
`知识库状态与准入规则V0.2.md`；`MVP产品需求文档.md`；`项目长期记忆.md`；`knowledge_base_outputs/第一批8配置端到端收口报告.md`。

---

## MPS-006：第一批 8 个配置如何处理 AMT 配置抽取失败、PCX160 功率证据不足等异常

**案例编号、日期/阶段、状态**  
MPS-006；2026-08-29；已解决。第一批 8 配置规则池已收口，异常均有降级或重建处理。

**问题现象与用户/业务影响**  
第一批规则池需要尽快形成可用于确定性筛选的最小集合，但其中存在两个典型异常：450CL-C AMT 的模型抽取配置名绑定失败；PCX160 最大功率只有单平台证据，不满足降级为 `provisional_verified` 的门槛。

**发现问题的证据**  
`knowledge_base_outputs/第一批8配置端到端收口报告.md` 记录 AMT 的 `trim_name` 从官方单配置证据确定性重建，未使用失败模型输出；PCX160 功率置 `null`，HSTC 细分不参与硬规则。`scripts/validate-eligible-pool-v02.mjs` 专门检查 PCX160 功率必须保持 null，AMT 必须带有确定性重建标记。

**根因分析**  
AMT 问题的根因是模型对配置名字段绑定不稳定，不能因为 JSON 可解析就放行。PCX160 问题的根因是功率字段证据不足，不应以单平台值进入硬规则或语义推导。

**当时考虑过的方案与取舍**  
AMT 可以继续重试模型、人工填补或确定性重建。继续重试已显示收益低，因此选择从已核验官方单配置证据确定性重建。PCX160 可以使用单平台功率、用模型补全或置空降级。当前选择置空，保证不把未核验字段带入硬规则。

**最终决策及为什么选择**  
AMT：保留车型进入规则池，但配置名来源必须是确定性重建，并标记模型抽取未用于配置名。PCX160：允许在售、价格等硬准入字段通过，但最大功率保持 `null`，相关动力档、操作压力和新手友好保持 `unknown`。

**实施动作或关联产物**  
正式规则池为 `knowledge_base_outputs/eligible_pool/first_batch_v1.json`。异常记录为 `knowledge_base_outputs/exceptions/first_batch_closure_exceptions.json`。校验脚本为 `scripts/validate-eligible-pool-v02.mjs`。

**验证方法与结果**  
第一批报告记录：8 条全部 `rule_pool_eligibility=eligible`，`blocked=0`，`pending_deterministic_check=0`，错误放行 0。本文创建时再次运行校验脚本，结果同样为 valid true、eligible 8、approved 0、errors []。

**尚存风险、后续触发条件**  
风险是后续排序或解释误把 PCX160 的未知动力字段当作中性以上优势。触发条件是语义排序接入、AI 解释生成或新增 PCX160 证据。

**可迁移的 AI 产品经理方法**  
异常处理不是“修到看起来完整”，而是决定哪些字段可以降级、哪些字段必须为空、哪些字段可用确定性证据重建。宁可缺失，也不要伪完整。

**面试表达版本**  
30 秒摘要：第一批 8 配置里我没有让模型失败字段强行过关。AMT 配置名用官方单配置证据确定性重建，PCX160 功率证据不足就置 null，并让相关语义保持 unknown。  
追问可展开：可以讲字段是否硬准入、null 和缺键的区别、以及为什么这仍可进入规则池但不代表推荐批准。

**相关文件**  
`knowledge_base_outputs/第一批8配置端到端收口报告.md`；`knowledge_base_outputs/eligible_pool/first_batch_v1.json`；`knowledge_base_outputs/exceptions/first_batch_closure_exceptions.json`；`scripts/validate-eligible-pool-v02.mjs`。

---

## MPS-007：为什么 `eligible` 不等于 `recommendation approved`

**案例编号、日期/阶段、状态**  
MPS-007；2026-08-28 至 2026-08-29；已解决。

**问题现象与用户/业务影响**  
第一批 8 条配置已进入确定性规则池，容易被误写成“可以对外推荐”。这会绕过产品层推荐复核，给用户展示尚未批准的推荐结果。

**发现问题的证据**  
`知识库状态与准入规则V0.2.md` 明确禁止从 `eligible` 推导 `approved`。`knowledge_base_outputs/第一批8配置端到端收口报告.md` 写明 `recommendation approved=0`。`rules-engine.js` 默认过滤未 approved 车型，只有 `allowUnapprovedPreview=true` 才进入开发预览。

**根因分析**  
已确认根因是“数据能参加规则筛选”和“产品允许对外推荐”属于不同决策层。前者是数据与程序门槛，后者需要推荐层复核与产品批准。

**当时考虑过的方案与取舍**  
如果把 eligible 直接当可推荐，开发快但风险大。当前方案默认生产推荐只放 approved，开发预览可用 eligible，但明确 `recommendation_allowed=false`。

**最终决策及为什么选择**  
决策是：`eligible` 只进入确定性候选池；`approved` 才进入最终推荐池。Codex review、模型抽取通过、字段事实通过都不能自动推出 approved。

**实施动作或关联产物**  
V0.2 文档、规则池 manifest、`rules-engine.js` 和 `scripts/test-rules-engine.mjs` 都围绕该边界实现。测试中生产结果对未 approved 池返回 `no_eligible_candidate`，开发预览才返回候选。

**验证方法与结果**  
`scripts/test-rules-engine.mjs` 运行通过 7 个场景，其中 productionResult 的 candidate_count 为 0，previewResult 可返回候选但 recommendation_allowed 为 false。规则池校验结果 approved 仍为 0。

**尚存风险、后续触发条件**  
风险是前端或后端调用时误开 `allowUnapprovedPreview`。触发条件是接入真实推荐接口、上线环境配置或面试演示数据切换。

**可迁移的 AI 产品经理方法**  
候选池、推荐池和展示池必须分层。AI 产品经理需要定义“什么时候可以内部使用，什么时候可以对用户说出口”。

**面试表达版本**  
30 秒摘要：我把 eligible 和 approved 分开，eligible 只是规则可筛选，不代表可对外推荐。规则引擎默认只返回 approved，未批准数据只能开发预览，避免把数据准入误包装成产品背书。  
追问可展开：可以讲生产过滤、开发预览开关、Codex review 与 Ethan 产品批准的区别。

**相关文件**  
`知识库状态与准入规则V0.2.md`；`knowledge_base_outputs/第一批8配置端到端收口报告.md`；`rules-engine.js`；`scripts/test-rules-engine.mjs`。

---

## MPS-008：语义增强如何区分事实、确定性推导和产品假设，并处理中性缺失

**案例编号、日期/阶段、状态**  
MPS-008；2026-08-29；部分解决。语义 V0.1 已建立并校验通过，真实用户排序效果待验证。

**问题现象与用户/业务影响**  
规则池只有硬字段还不够做“像人一样”的排序，但语义字段如果写得太主观，会把产品假设伪装成车型事实。例如“新手友好”“维护便利”“操作压力”都容易被误解释为客观结论。

**发现问题的证据**  
`knowledge_base_outputs/semantic_enrichment/语义字段V0.1说明.md` 将字段分为 `deterministic_derived`、`source_supported`、`product_hypothesis` 和 `unknown`。`knowledge_base_outputs/semantic_enrichment/第一批语义增强V0.1收口报告.md` 记录维护便利性 8 条全部 unknown，PCX160 因功率为 null 相关字段保持 unknown。

**根因分析**  
已确认根因是部分排序信号来自可测参数推导，部分来自产品策略假设，不能混同为事实。维护/售后便利性缺服务网络、配件和维护成本证据，不能由模型常识补全。

**当时考虑过的方案与取舍**  
方案 A 是允许低权重进入内部排序，用用户测试校准。方案 B 是暂不接入语义，只按硬字段和价格排序。方案 C 是让 AI 自由解释语义。当前 Ethan 选择方案 A，但这只是产品策略批准，不是逐字段事实核验或 recommendation approval。

**最终决策及为什么选择**  
决策是：用途标签、风格标签、已知动力档、已知操作压力和新手友好候选值可低权重进入内部排序；维护便利性不参与排序；PCX160 的未知字段中性缺失，不插补、不加分也不扣分。

**实施动作或关联产物**  
新增语义层目录 `knowledge_base_outputs/semantic_enrichment/`，包含语义数据、schema、manifest、exceptions、说明文档和收口报告。`rules-engine.js` 中语义信号为低权重内部排序信号，且 `semantic_score_user_visible=false`。

**验证方法与结果**  
本文创建时运行 `scripts/validate-semantic-enrichment-v01.mjs`，结果为 8 条有效；usage 8、pressure 7、beginner 7、style 8、power 7、maintenance 0；recommendation_approved_count 0；model_api_calls 0。`scripts/test-rules-engine.mjs` 验证 PCX160 只使用 usage_tags，不因 unknown 获得新手友好理由。

**尚存风险、后续触发条件**  
风险是 AI 解释把内部语义分说成客观安全保证。触发条件是接入解释生成、对外展示推荐理由，或新增维护/口碑数据。

**可迁移的 AI 产品经理方法**  
语义增强要写清楚来源类型：事实、规则推导、产品假设、未知。缺失值处理必须提前定义，否则排序系统会悄悄奖励或惩罚没有证据的车型。

**面试表达版本**  
30 秒摘要：我没有让 AI 直接给车型贴主观标签，而是把语义层拆成确定性推导、来源支持、产品假设和 unknown。未知字段保持中性，PCX160 功率缺证据就不生成动力和新手友好结论。  
追问可展开：可以讲低权重内部排序、为什么维护便利性 0 条参与排序、以及为什么产品策略批准不是事实核验。

**相关文件**  
`knowledge_base_outputs/semantic_enrichment/语义字段V0.1说明.md`；`knowledge_base_outputs/semantic_enrichment/第一批语义增强V0.1收口报告.md`；`scripts/validate-semantic-enrichment-v01.mjs`；`rules-engine.js`。

---

## MPS-009：为什么采用“规则筛选 + 低权重语义排序 + AI 解释”，而不是让模型自由推荐

**案例编号、日期/阶段、状态**  
MPS-009；2026-08-27 至 2026-08-29；部分解决。架构原则与规则引擎初版已存在，完整线上链路待实现。

**问题现象与用户/业务影响**  
自由推荐看起来智能，但容易出现超预算、停产车型、价格过期、参数编造、品牌偏袒和解释无法追溯。摩托车选购直接影响用户花钱和安全边界，不能只靠模型记忆。

**发现问题的证据**  
`MVP产品需求文档.md` 明确采用“规则筛选 + AI 解释”。`system_prompt.md` 要求先核实再推荐、不要编造当前价格、不绕过预算。`rules-engine.js` 实现了 eligible、在售、预算、车型类型和 approved 的确定性过滤，并把语义信号作为内部低权重排序。

**根因分析**  
已确认根因是推荐质量依赖硬约束和可信数据。预算、在售状态、价格口径和数据可信度不能由模型自由裁量，也不能被其他软性维度高分抵消。

**当时考虑过的方案与取舍**  
纯 LLM 推荐开发快，但难以保证事实和边界。纯规则推荐稳定但解释僵硬，对自然语言理解弱。混合方案让规则负责准入与排序，AI 负责需求理解、低打扰追问和解释。

**最终决策及为什么选择**  
决策是规则筛选生成可审计候选池，语义层只低权重参与内部排序，AI 不得绕过候选池推荐未经核验车型。这样在可信度和体验之间取得平衡。

**实施动作或关联产物**  
`rules-engine.js`、`scripts/test-rules-engine.mjs`、`MVP产品需求文档.md` 和 `system_prompt.md` 共同定义该机制。当前规则引擎测试覆盖生产过滤、开发预览、预算、车型类型和语义排序。

**验证方法与结果**  
`scripts/test-rules-engine.mjs` 通过 7 个场景。规则引擎默认未 approved 不可推荐；开发预览可查看候选但 recommendation_allowed 为 false；语义分不对用户可见。

**尚存风险、后续触发条件**  
风险是完整 AI 解释层尚未接入，未来 prompt 或前端可能绕过规则池。触发条件是实现推荐 API、接入模型解释或临时候选联网搜索。

**可迁移的 AI 产品经理方法**  
AI 不一定要做最终决策者。高风险推荐场景可以采用“确定性规则定边界，AI 做理解和解释”的系统设计。

**面试表达版本**  
30 秒摘要：我没有让模型自由推荐，而是让规则先筛预算、在售和数据可信度，语义只低权重排序，AI 负责把取舍讲明白。这样既有自然语言体验，又能控制事实和越界风险。  
追问可展开：可以讲预算不能被颜值高分抵消、临时候选必须核验、以及 AI 解释不能绕过候选池。

**相关文件**  
`MVP产品需求文档.md`；`system_prompt.md`；`rules-engine.js`；`scripts/test-rules-engine.mjs`。

---

## MPS-010：为什么网页原型接入曾被判断过早，随后把 Agent 需求与架构设计前置

**案例编号、日期/阶段、状态**  
MPS-010；2026-08-27 至 2026-08-30；部分解决。需求与架构设计基线已完成 11/11 阶段确认并合并进主项目，但工程实现、PRD/System Prompt 同步和真实用户验证尚未完成。

**问题现象与用户/业务影响**  
仓库中存在 `index.html`、`styles.css`、`app.js` 和原型图片，但长期记忆明确记录：此前误生成的静态网页原型暂时保留，需求确认完成前不继续开发，原型内容不视为已确认需求。若过早接入，会把 UI 变成需求来源。2026-08-30 最新阶段判断是：当前不是继续堆页面，而是先收口 MVP 实施前的设计基线，再进入 8 配置端到端内部 Alpha。

**发现问题的证据**  
`项目长期记忆.md` 记录首发形态为网页应用，同时说明误生成静态网页原型不继续开发。`MVP产品需求文档.md` 的决策记录写明当前没有阻塞 MVP 开发的产品待确认项，但模型供应商、部署平台、技术栈和数据表结构在技术方案阶段确定。《Agent需求与架构设计V0.1》已按 Ethan 要求分 11 个阶段逐步确认，11/11 均为 `Ethan_confirmed`，不是一次性替 Ethan 做决策；该文档已于 2026-08-30 合并进主项目，但尚未提交到 GitHub。

**根因分析**  
已确认根因是界面原型容易让团队提前关注页面展示，而真正影响可信度的是 Agent 流程、知识库准入、价格策略、评测体系和安全边界。新增根因是多窗口/多 worktree 并行推进会让“窗口里完成”和“主项目已合并”之间出现状态差，必须在档案中拆开记录。

**当时考虑过的方案与取舍**  
一种做法是先把网页原型做完整，能快速展示但会掩盖推荐可信度问题。另一种做法是先补 PRD、system prompt、知识库状态、规则引擎和 Agent 架构，再回到页面接入。当前选择后者，并把实施路线拆成：先用现有 8 个配置跑通端到端 `development_preview` 验证链路；稳定后扩至 12-15 款验证覆盖度；外部 MVP 再扩至约 25-35 款，并要求正式 recommendation approval。

**最终决策及为什么选择**  
决策是保留原型文件，但不让原型驱动需求；先完成 Agent 需求、知识库规则、评测口径和安全边界，再进入前端/后端接入。当前阶段定义为“MVP 实施前的设计基线收口”，下一阶段是 8 配置端到端内部 Alpha。内部 Alpha 的目标是验证链路，不证明车型覆盖充分。

**实施动作或关联产物**  
已形成 PRD、system prompt、知识库 V0.2、首批规则池、语义增强和规则引擎测试。Agent 架构文档已完成 11 阶段确认、自查和主项目合并，长期记忆已按阶段决策合并。下一步实施前置事项包括同步 PRD/System Prompt、建立 20 条工程冒烟评测。

**验证方法与结果**  
验证来自两层证据：当前 worktree 中，PRD 与长期记忆均明确前置需求和架构，且没有证据显示网页原型已接入真实推荐链路；主任务同步中，Agent 架构文档自查结果为 7 个 JSON 示例可解析、11 个阶段状态确认、代码围栏闭合、无疑似密钥。3 个 Mermaid 图仅做人工语法/围栏检查，因环境没有 Mermaid CLI，未完成渲染器验证。

**尚存风险、后续触发条件**  
风险是面试演示时误把静态原型当作可用产品，或把主项目中的未提交文档误写成 GitHub 已包含成果。触发条件是准备 demo、提交 GitHub、接入推荐 API、同步 PRD/System Prompt、建立 20 条冒烟评测或上线前端。

**可迁移的 AI 产品经理方法**  
原型不是需求真相，窗口完成也不等于主项目完成。AI 产品尤其要先证明决策链路可信，再让 UI 承载它；进入工程前应设置设计冻结点和合并门槛，避免不同文档、prompt 和实现各说各话。

**面试表达版本**  
30 秒摘要：我曾判断网页原型接入过早，因为核心风险不是页面，而是推荐链路是否可信。后来把 Agent 需求和架构拆成 11 个阶段让 Ethan 逐步确认，再计划用 8 个配置做内部 Alpha 跑通链路，而不是直接宣称车型覆盖充分。  
追问可展开：可以讲为什么原型保留但不当需求依据、为什么 development_preview 只验证链路、以及为什么独立 worktree 完成不等于主项目已合并。

**相关文件**  
`项目长期记忆.md`；`MVP产品需求文档.md`；`index.html`；`styles.css`；`app.js`；`Agent需求与架构设计V0.1.md`。

---

## MPS-011：为什么不盲目建设多 Agent，以及编排器 + 模块化工具的取舍

**案例编号、日期/阶段、状态**  
MPS-011；2026-08-27 至 2026-08-30；部分解决。实时架构已确认为一个编排器 + 模块化工具，完整工程实现待内部 Alpha。

**问题现象与用户/业务影响**  
多 Agent 听起来适合复杂咨询，但如果每轮都启动多个 Agent，会增加 Token 成本、延迟和上下文传递噪音。对用户来说，普通追问不应该等完整数据核验和解释生成都跑完。最新阶段还确认首轮二手只做文字助手，图片能力后移，因此“图片分析 Agent”不应在首轮被默认纳入实时链路。

**发现问题的证据**  
`MVP产品需求文档.md` 的非功能需求写明：采用受控的多 Agent/多任务并行，只并行处理独立窄任务；多 Agent 用于降低关键路径等待时间，不作为节省 Token 的默认手段。`项目长期记忆.md` 也记录编排器根据用户意图、缓存状态和输入类型动态路由。主任务同步的最新事实将实时架构进一步收口为“一个编排器 + 模块化工具”，独立并行只用于确实可拆分并能降低关键路径延迟的任务。

**根因分析**  
已确认根因是推荐系统中有些步骤适合并行，如来源核验和价格刷新；有些步骤必须串行或确定性执行，如预算准入、状态门禁和排序。图片处理虽然适合独立链路，但首轮已后移。盲目拆 Agent 会让系统更慢、更贵、更难验收。

**当时考虑过的方案与取舍**  
方案 A 是全量多 Agent，每次启动需求、核验、解释、图片等能力，体验不稳定且成本高。方案 B 是单 Agent 做全部事，简单但慢且边界不清。当前原则是一个编排器 + 模块化工具：只启动必要能力，确定性规则优先，并把独立并行限制在能降低关键路径延迟的任务上。

**最终决策及为什么选择**  
决策是受控并行与动态路由。普通追问不启动全部能力；缓存有效时不重复联网；预算筛选、准入条件、权重排序和结构化校验优先使用程序规则。评测也按分层策略：20 条工程冒烟用于代码、Prompt、规则或知识库变更后的快速回归；50 条黄金集用于上线前及模型、规则、推荐策略发生实质变化时的发布门控。

**实施动作或关联产物**  
目前该架构已写入 PRD、长期记忆、规则引擎雏形及已合并的 Agent 架构文档。完整编排器、模块化工具接口、20 条工程冒烟评测和 50 条黄金集发布门控尚未在主项目中完成。

**验证方法与结果**  
已验证的是产品架构口径，不是工程实现。当前 worktree 没有证据显示编排器已上线或完成代码实现。主任务同步的架构文档自查确认 7 个 JSON 示例可解析、11 个阶段状态确认、代码围栏闭合、无疑似密钥；Mermaid 图未完成渲染器验证。

**尚存风险、后续触发条件**  
风险是后续为了展示“Agent 感”过度拆分模块，或把图片能力提前拉回首轮实时链路。触发条件是设计后端架构、接入联网核验、建立冒烟评测、接入图片分析或成本监控。

**可迁移的 AI 产品经理方法**  
不要把多 Agent 当目标，而要问：这一步是否独立、是否可并行、是否减少关键路径、是否增加可验证性。实时架构可以用一个编排器保持用户体验一致，用模块化工具承接可替换能力；能用确定性工具解决的，不要交给 Agent 反复推理。

**面试表达版本**  
30 秒摘要：我没有盲目做多 Agent，而是把实时架构收敛成一个编排器加模块化工具。预算和准入用确定性规则，只有来源核验、价格刷新这类可拆分任务才并行；图片能力首轮后移，避免把未验证的视觉链路塞进关键路径。  
追问可展开：可以讲普通追问不启动全链路、缓存命中不重复核验、20 条冒烟与 50 条黄金集的分层评测，以及为什么图片能力等用户测试后再建。

**相关文件**  
`MVP产品需求文档.md`；`项目长期记忆.md`；`rules-engine.js`；`Agent需求与架构设计V0.1.md`。

---

## MPS-012：GitHub 账号迁移、私有仓库、密钥排除与版本管理中的安全实践

**案例编号、日期/阶段、状态**  
MPS-012；2026-08-28 至 2026-08-29；部分解决。密钥排除和私有开发空间有证据；GitHub 账号迁移过程缺少仓库内完整证据，标记为待验证。

**问题现象与用户/业务影响**  
项目接入智谱、豆包等模型能力时，会产生 API Key、Token、验证码、账号信息和仓库权限风险。如果密钥进入聊天、文档或 Git 历史，后续公开作品或协作会带来泄露风险。

**发现问题的证据**  
`.gitignore` 忽略 `.env` 和 `.env.*`，但保留 `!.env.example`。`git ls-files` 仅显示 `.env.example`，没有 `.env`。`.env.example` 只包含变量名和空值。`README.md` 写明真实 API Key 仅保存在本机 `.env`，`.env.example` 只保留变量名和示例配置。多份模型报告记录 API Key 仅由本机 `.env` 加载，未读取、打印或写入产物。当前 `git remote -v` 输出未显示嵌入式 token。关于 GitHub 账号迁移本身，当前仓库没有足够过程证据，不能写成已完成事实。

**根因分析**  
已确认根因是 AI 项目容易把“能跑通 API”与“安全可提交”混在一起。账号迁移和私有仓库设置属于外部平台状态，单靠本地文件不能完整证明。

**当时考虑过的方案与取舍**  
一种做法是把真实配置写进文档或脚本，运行方便但不可接受。当前做法是只提交 `.env.example`，真实密钥仅由本机环境变量或本地 `.env` 读取，报告中只记录模型名、usage、HTTP 状态和输出路径。

**最终决策及为什么选择**  
决策是：不在仓库、聊天和文档中记录真实密钥、验证码、邮箱、Token 或账号迁移敏感细节；版本管理只提交占位配置和过程证据；公开前进一步检查访谈材料隐私和过程文件。

**实施动作或关联产物**  
关联产物包括 `.gitignore`、`.env.example`、`README.md`、`V0.1首批车型知识库草案.md` 和模型路由报告。本文也不记录真实账号、邮箱、验证码、API Key 或 Token。

**验证方法与结果**  
本次核查执行了文件检索、`git ls-files` 和 `git remote -v`。结果显示 `.env` 未被 Git 跟踪，`.env.example` 是唯一环境示例文件；远程地址未出现嵌入式凭据。未验证项：GitHub 仓库是否始终为私有、账号迁移具体步骤、历史远程权限变更。

**尚存风险、后续触发条件**  
风险是历史提交或未来公开前仍包含敏感访谈信息、私有账号痕迹或过程文件。触发条件是迁移仓库、公开作品、邀请协作者、生成发布包或接入 CI/CD。

**可迁移的 AI 产品经理方法**  
安全不是上线前最后一步，而是从第一条 API 调用开始记录边界。能提交的是配置模板和审计结果，不能提交的是凭据和外部账号私密信息。

**面试表达版本**  
30 秒摘要：我把密钥和版本管理分开处理：真实 Key 只在本机 `.env`，仓库只提交 `.env.example`，模型报告只记录 usage 和校验结果，不记录凭据。GitHub 账号迁移过程当前证据不足，所以我只记录可证实的工程安全实践。  
追问可展开：可以讲 `.gitignore`、公开前隐私检查、远程地址不含 token，以及为什么不把账号邮箱或验证码写进复盘。

**相关文件**  
`.gitignore`；`.env.example`；`README.md`；`V0.1首批车型知识库草案.md`；`knowledge_base_outputs/GLM45Air_GLM46V_GLM47_同输入ABC基准报告.md`。

---

## MPS-013：智谱权益剩余窗口、资源额度与高 Token 任务使用边界

**案例编号、日期/阶段、状态**  
MPS-013；2026-08-29；部分解决。资源信息已纳入执行策略和事实边界，但到期日、每日重置规则和平台账单抵扣仍未由本任务独立核验。

**问题现象与用户/业务影响**  
项目在知识库扩建、资料清洗、证据整理、批量抽取和评测初筛阶段可能消耗大量 Token。若忽略当前权益窗口，会错过低成本推进窄任务的机会；但如果为了消耗额度扩大 MVP 范围，又会破坏产品收敛和事实可信边界。

**发现问题的证据**  
主任务于 2026-08-29 同步 Ethan 的账户反馈：智谱权益约还剩 6 天，资源包每日重置；但同步信息同时指出截图本身未显示到期日或每日重置规则，因此这两点不能写成平台独立核验事实。主任务同步的截图摘要可确认当时资源包均为生效中，页面约显示：`glm-4.7` 5,000,000 tokens、`glm-4.6v` 6,000,000 tokens、`glm-4.5-air` 11,978,637 tokens、通用按 Token 1,785,656 tokens，另有 86 次搜索和 20 次按次调用。本任务当前未直接读取截图原件，故只按“主任务同步证据/账户反馈”记录。

**根因分析**  
已确认的产品工程问题是：资源额度会影响批量任务排期，但不应改变模型路由、MVP 范围或事实准入标准。未验证假设是“约 6 天到期”和“每日重置”这两个账户规则，仍需以平台页面、账单或权益明细为准。

**当时考虑过的方案与取舍**  
方案 A 是主动扩大采集和评测规模以消耗额度，短期产出多但会引入范围蔓延。方案 B 是完全不考虑权益窗口，稳但可能浪费已配置资源。当前取舍是只在确有高 Token 窄任务时优先使用本机已配置的智谱 API，并继续受模型基准、校验和熔断约束。

**最终决策及为什么选择**  
决策是：资料采集、证据整理、批量抽取、评测初筛等窄任务可以优先使用智谱 API；当前若只是产品确认、文档结构设计或问题档案维护，不为额度主动调用模型。文本抽取继续优先考虑已通过基准的 `glm-4.7`；涉及图片时 `glm-4.6v` 仍需按具体视觉任务验证，不能只因额度更多认定最佳。

**实施动作或关联产物**  
本次只更新问题档案的执行策略和事实边界，没有调用智谱模型。沿用既有约束：usage 日志、单请求上限、最多重试、连续失败熔断、来源白名单、失败降级、模型输出仅为 `extraction_draft`。

**验证方法与结果**  
本任务执行了本地检索，未发现该资源窗口信息已写入既有项目文件。既有模型基准仍支持文本抽取优先 `glm-4.7`，并支持“额度不是默认模型选择主因”的结论。本次未调用 API，因此没有新增 usage。

**尚存风险、后续触发条件**  
风险是到期日、每日重置和实际抵扣规则与账户反馈不一致。触发条件是启动大批量采集、扩建知识库、跑评测初筛、调用搜索次数资源，或接近权益窗口结束。

**可迁移的 AI 产品经理方法**  
资源窗口可以影响执行节奏，但不能反向定义产品范围。AI 产品经理应把“成本机会”纳入排期，把“事实准入”和“用户价值”作为不被额度改变的硬边界。

**面试表达版本**  
30 秒摘要：我把智谱剩余额度当作执行资源，而不是产品方向。高 Token 的窄任务可以优先用本机 API，但不因为额度还在就扩大 MVP，也不让模型输出替代来源核验和规则校验。  
追问可展开：可以讲账户反馈与平台核验的区别、为什么文本仍按 glm-4.7 基准路由、以及为什么图片模型不能只看额度。

**相关文件**  
`knowledge_base_outputs/GLM45Air_GLM46V_GLM47_同输入ABC基准报告.md`；`knowledge_base_outputs/V0.1_P0五检索单元扩建批次报告.md`；`V0.1首批车型知识库草案.md`；`.env.example`。

---

## MPS-014：如何通过阶段确认形成设计冻结点，并从架构设计进入内部 Alpha

**案例编号、日期/阶段、状态**  
MPS-014；2026-08-30；部分解决。设计基线已完成 11/11 阶段确认并合并进主项目；当前处于“MVP 实施前的设计基线收口”，下一阶段是 8 配置端到端内部 Alpha。工程实现和 GitHub 同步尚未完成。

**问题现象与用户/业务影响**  
项目已经有 PRD、system prompt、知识库、规则池、语义层和原型文件，如果不设置设计冻结点，下一步开发容易在范围、数据、Agent 架构和评测口径上来回摆动。另一方面，架构文档和问题档案位于独立 Git worktree 时，“窗口完成”容易被误认为“主项目已合并”，造成交付状态误判。

**发现问题的证据**  
2026-08-30 核查确认：《Agent需求与架构设计V0.1》已按 Ethan 要求分 11 个阶段逐步确认，11/11 均为 `Ethan_confirmed`，不是一次性替 Ethan 做决策。架构文档和问题档案已从独立 worktree 合并进主项目，长期记忆已逐项合并；PRD/System Prompt 同步、20 条冒烟评测和 GitHub 提交仍未完成。

**根因分析**  
已确认根因是项目从探索进入实施前，需要一个可追踪的设计冻结点。该冻结点不仅包括“做什么”，也包括“不做什么”“用什么数据范围验证”“用什么评测门控”“哪些文件尚未合并”。若不显式记录，后续工程会混用旧 PRD、旧 prompt 和新架构设想。

**当时考虑过的方案与取舍**  
方案 A 是直接进入全量 MVP 开发，速度快但容易把旧 PRD 中的图片能力、25-35 款覆盖和推荐批准混在一起。方案 B 是先把 8 配置端到端跑通作为内部 Alpha，只验证链路稳定性，不证明覆盖充分。当前选择方案 B，并设置后续扩展节奏：稳定后扩至 12-15 款验证覆盖度；外部 MVP 再扩至约 25-35 款，并要求正式 recommendation approval。

**最终决策及为什么选择**  
决策是：以 11 阶段确认并已合并的 Agent 架构作为实施前设计基线。内部 Alpha 只使用现有 8 个配置跑通 `development_preview`，目标是验证需求识别、规则筛选、语义低权重排序、AI 解释、证据展示和反馈链路，而不是证明车型覆盖充分或允许正式对外推荐。

**实施动作或关联产物**  
已完成架构文档 11/11 阶段确认、主项目合并和长期记忆逐项合并；自查包含 7 个 JSON 示例可解析、代码围栏闭合、无疑似密钥。待实施动作包括同步 PRD/System Prompt、建立 20 条工程冒烟评测和提交 GitHub。当前知识库仍只有 8 个配置可用于内部 Alpha，`recommendation approved` 仍为 0。

**验证方法与结果**  
架构文档自查结果：7 个 JSON 示例可解析；11 个阶段状态确认；代码围栏闭合；无疑似密钥。3 个 Mermaid 图仅完成现场人工语法/围栏检查，因为环境没有 Mermaid CLI，未完成渲染器验证。主项目已能检索到架构文档和问题档案，但尚未提交 GitHub。

**尚存风险、后续触发条件**  
风险包括：主项目未提交导致 GitHub 缺失最新设计；PRD 和 System Prompt 仍保留过期图片首发设想；20 条冒烟评测尚未建立；8 配置 Alpha 被误解为车型覆盖充分；未批准推荐被误当作正式推荐。触发条件是开始内部 Alpha、提交文档、同步 PRD/System Prompt、扩展到 12-15 款或准备外部 MVP。

**可迁移的 AI 产品经理方法**  
复杂 AI 产品进入开发前，需要把探索结论冻结成可执行基线：阶段确认、实施路线、数据范围、评测分层、未合并风险和不可宣称边界都要写清。设计冻结不是停止迭代，而是防止工程在多个版本的事实之间漂移。

**面试表达版本**  
30 秒摘要：我没有一次性替用户拍板架构，而是把 Agent 需求和架构拆成 11 个阶段逐步确认，形成实施前设计基线。下一步先用 8 个配置跑 development_preview 内部 Alpha，只验证链路，不证明覆盖充分；稳定后再扩到 12-15 款和外部 MVP 的 25-35 款。  
追问可展开：可以讲 worktree 未合并风险、20 条冒烟和 50 条黄金集的门控区别、为什么 recommendation approved 仍为 0，以及为什么 PRD/System Prompt 需要同步旧图片设想。

**相关文件**  
`Agent需求与架构设计V0.1.md`；`项目问题与解决方案档案.md`；`MVP产品需求文档.md`；`system_prompt.md`；`knowledge_base_outputs/eligible_pool/first_batch_v1.json`；`rules-engine.js`。

---

## MPS-015：库外车型联网发现必须经过双源证据与预算门禁

**案例编号、日期/阶段、状态**  
MPS-015；2026-08-31；部分解决。真实联网正反向路径和安全门禁已通过本地验收，但仍是内部开发预览，尚无生产监控与正式推荐批准。

**问题现象与用户/业务影响**  
用户只会描述预算、用途和偏好，不会主动要求系统查询某个库外车型。当本地知识库覆盖不足时，如果 Agent 不联网，会漏掉适合的新车型；如果把搜索摘要直接当事实，又可能推荐错配置、错价格或超预算车型。

**发现问题的证据**  
1.8 万元巡航场景搜索到春风 250CL-C 的平台价格线索，但允许域名内缺少品牌官网车型证据，系统返回 `external_verification_required` 且不放行。另一条真实路径中，豪爵 TVL350 泛车型只能在官网形成“价格紧邻配置名”的明确配对后收敛为扶手版，并由官网与摩托范同年款、同配置证据核验为 28,980 元。

**根因分析**  
搜索结果是发现线索，不是可直接推荐的事实。官网信息还可能分散在车型概览、配置价格和参数页面；若仅按文本距离或反向顺序绑定价格，容易把一个配置的价格错配给另一个配置。

**当时考虑过的方案与取舍**  
只用本地知识库最稳，但覆盖不足；直接采用搜索摘要覆盖广，但事实风险不可控。最终采用“通用搜索 + 最多 2 次车型类型相关品牌定向补搜 + 官网多页面证据包 + 主流平台同配置价格 + 核验后预算二次门禁”。用户指定车型类型时，类型未知草稿不得进入核验。

**最终决策及为什么选择**  
库外候选只有品牌官网完成车型身份、在售状态和关键参数核验，同时主流平台完成同配置、同口径价格核验后，才可成为 `verified_temporary_candidate`。它仍不是正式知识库记录或正式推荐；座高未知时不得推断适配身高，价格超过用户预算时必须撤销。

**实施动作或关联产物**  
主要产物为 `external-candidate-discovery.js`、`web-evidence-fetcher.js`、`external-evidence-pipeline.js`、`bocha-search-client.js` 与 Alpha API 接入。抓取器限制允许域名、DNS 私网地址、重定向次数、页面体积并检测可疑提示注入。

**验证方法与结果**  
博查客户端 17 场景、证据管线 30 场景、自动搜索集成 9 场景通过。250CL-C 证明缺官网证据会阻断；TVL350 扶手版证明多页面官网证据和同配置价格可以形成临时候选；核验后的预算二次门禁也有独立回归案例。

**尚存风险、后续触发条件**  
仍缺生产环境的搜索稳定性、页面变化、来源失效、时延和费用监控。触发条件是开始 10 至 20 人外部邀请 Alpha 或允许真实用户依赖库外候选。

**可迁移的 AI 产品经理方法**  
把联网检索拆成“发现”和“核验”两个阶段，分别设计准入标准与失败降级。搜索扩大召回，证据门禁保护准确率，两者不能混成一次模型回答。

**面试表达版本**  
30 秒摘要：我没有让 Agent 直接相信搜索摘要，而是把库外推荐拆成候选发现、官网与平台双源核验、配置价格绑定和预算二次门禁。250CL-C 因缺官网证据被阻断，TVL350 扶手版在证据齐全后也只作为临时候选。  
追问可展开：可以讲为什么官网需要多页面证据包、为什么禁止反向价格绑定，以及为什么临时候选不等于正式推荐。

**相关文件**  
`external-evidence-pipeline.js`；`external-candidate-discovery.js`；`web-evidence-fetcher.js`；`bocha-search-client.js`；`scripts/check-external-evidence-positive-path.mjs`。

---

## MPS-016：50 条黄金评测发现落地预算仍按裸车预算筛选

**案例编号、日期/阶段、状态**  
MPS-016；2026-08-31；已解决。预算传递漏洞已修复并通过黄金评测、工程冒烟和页面回归。

**问题现象与用户/业务影响**  
用户输入“3 万元落地预算”后，需求层已扣除上牌、保险和装备预留，算出 26,500 元可用裸车预算；但推荐管线仍把原始 30,000 元交给规则筛选，导致 27,800 元的 NMAX155 被错误纳入。

**发现问题的证据**  
50 条黄金集首次执行时暴露了需求计算与推荐筛选之间的参数传递不一致。`decision-core` 已生成 `filter_budget_cny`，而规则引擎、预算分层和最近候选仍优先读取原始预算。

**根因分析**  
同一个“预算”在产品中有展示口径和决策口径两个含义：原始预算用于让用户确认，扣除费用预留后的预算用于筛车。此前接口没有把两者显式分开，造成下游误用。

**最终决策及为什么选择**  
保留原始预算用于对话展示，同时将 `filter_budget_cny` 传入规则筛选、预算分层和最近候选计算。这样既不篡改用户表达，也保证推荐遵守真实可支配购车预算。

**实施动作或关联产物**  
修订 `rules-engine.js`、`candidate-coverage.js` 和 `recommendation-pipeline.js` 的预算读取逻辑；新增 `evaluations/golden/golden_cases_v0.1.json`、说明文档与 `scripts/run-golden-evals.mjs`。黄金集绑定 17 配置规则池和语义层，覆盖对话、意图、推荐、覆盖度、预算、价格、二手、库外证据和费用。

**验证方法与结果**  
黄金评测 50/50、工程冒烟 20/20、编排器 11 场景、Alpha API 13 场景通过。页面回归确认 3 万元落地预算不再出现 27,800 元 NMAX155，而 3 万元裸车预算仍可出现该车。

**尚存风险、后续触发条件**  
当前费用预留仍是产品规则，不是地区化实时报价。后续支持城市差异、分期、购置税或保险实时计算时，需要重新扩充预算黄金案例。

**可迁移的 AI 产品经理方法**  
黄金评测不仅验证模型回答，也能验证跨模块产品契约。对“预算、价格、评分”等有多种口径的字段，应在 Schema 中显式命名，避免同名参数在链路中悄悄改变含义。

**面试表达版本**  
30 秒摘要：我用 50 条黄金集发现一个跨模块漏洞：3 万元落地预算虽然算出了 2.65 万裸车上限，下游仍按 3 万筛车。我把展示预算和筛选预算拆开传递，修复后黄金集 50/50，并完成页面回归。  
追问可展开：可以讲为什么单元测试没先发现、黄金集如何覆盖产品契约，以及为什么不能直接覆盖用户原始预算。

**相关文件**  
`evaluations/golden/golden_cases_v0.1.json`；`evaluations/golden/README.md`；`scripts/run-golden-evals.mjs`；`rules-engine.js`；`candidate-coverage.js`；`recommendation-pipeline.js`。

---

## MPS-017：正式推荐批准批次与外部邀请 Alpha 发布边界

**案例编号、日期/阶段、状态**  
MPS-017；2026-08-31；部分解决。责任机制、记录 Schema 和实时门禁已建立，但尚未创建首个真实批准批次，正式批准数仍为 0。

**问题现象与用户/业务影响**  
“首批正式推荐批准批次”容易被误解为又扩一批知识库，或者由 Ethan 逐车逐字段审核。若边界不清，内部开发预览、Codex 运营复核和产品负责人发布批准会被混为一谈，造成对外推荐风险。

**根因分析**  
车型事实准入与产品发布责任是两件事。`eligible` 说明记录可以进入规则筛选，Codex operational review 说明完成了工程与证据复核，但只有一次可审计的批次发布决定才能把指定版本升级为 `approved`。

**最终决策及为什么选择**  
拟发布批次先通过确定性门禁全量检查，再由 Codex 复核全部拟发布记录；Ethan 对批次做 10% 确定性随机抽样，并确认没有未解决政策变更、官方价格或在售冲突后，按批次批准发布。该决定是产品发布责任，不代表 Ethan 人工核验了全部事实字段。

**实施动作或关联产物**  
已建立 `recommendation_approval_batch_v0.1.schema.json`，用于记录版本范围、全量门禁报告、Codex 复核、随机抽样、阻塞项和产品负责人决定。外部邀请 Alpha 实时门禁已把 `recommendation_batch_approved` 与 50 条黄金评测、认证、云数据库等条件分开检查。

**验证方法与结果**  
50 条黄金评测已经通过并被门禁识别，但 `mvp_eligible_pool_v0.1` 的 `recommendation_approved_count=0`，因此外部邀请 Alpha 仍为未就绪。当前未创建真实批准记录，没有把开发预览误升级为正式推荐。

**尚存风险、后续触发条件**  
外部 Alpha 仍缺认证、云数据库与备份、用户删除、邀请码与容量、每日限额、真实费用计量和管理接口保护。触发条件是完成首批批次审查或准备把链接发给非陪同用户。

**可迁移的 AI 产品经理方法**  
把“数据可用”“运营复核”“发布批准”“系统可上线”拆成不同状态与门禁。这样每个角色只承担可解释的责任，也不会因为某一项测试通过就误判整个产品可发布。

**面试表达版本**  
30 秒摘要：正式推荐批准批次不是扩库，而是一次有范围、有证据、有责任人的发布决策。系统先全量跑确定性门禁，Codex 复核全部记录，我设计产品负责人做 10% 确定性抽样并按批批准；目前批准数仍为 0，所以外部 Alpha 还不能开放。  
追问可展开：可以讲为什么不要求逐字段人工审核、批准 Schema 记录什么，以及黄金集通过为什么仍不等于可上线。

**相关文件**  
`knowledge_base_outputs/recommendation_approvals/recommendation_approval_batch_v0.1.schema.json`；`knowledge_base_outputs/eligible_pool/mvp_eligible_pool_v0.1.json`；`scripts/check-alpha-test-readiness.mjs`；`testing/internal-alpha-readiness.json`。

---

## MPS-018：跨任务只回复“好的”但没有执行

**案例编号、日期/阶段、状态**
MPS-018；2026-09-01；部分解决。当前改为由主任务直接核查共享工作区产物，不再把“消息已发送”当成执行完成；Codex 跨任务可靠回执仍依赖平台能力。

**问题、根因与影响**
主任务向 UI、知识库或文档任务发送工作要求后，对方有时只回复“好的”，没有产生文件修改、测试结果或可验证产物。根因是自然语言确认与任务状态没有形成强事务契约，且消息送达、开始执行、完成验收是三个不同状态。若主任务直接汇报“已同步”，Ethan 仍需手动复制和复查，协作成本反而增加。

**解决方案与验证**
以后跨任务只用于独立工作；主任务必须通过 `git diff`、目标文件、测试输出或任务完成状态验收。涉及同一共享文件或需要马上闭环的改动，由主任务直接实施。本轮记忆 UI 即采用该方案，桌面和 390px 手机端实际验证通过。

**可迁移经验 / 面试表达**
Agent 协作需要可观察的完成定义：acknowledged 不等于 started，started 不等于 delivered。30 秒摘要：我把跨 Agent 协作从“发消息”改成“产物 + 测试 + 状态”三重验收，避免伪完成。

---

## MPS-019：Responses continuation 缺少 `call_id` 的协议错误

**案例编号、日期/阶段、状态**
MPS-019；2026-09-01；部分解决。通过重启和新建任务恢复，Ethan 选择方案 A，不改动或停用现有中转站；自动恢复机制尚未实现。

**问题、根因与影响**
任务续跑时出现 `function_call_output requires call_id on HTTP requests; continuation via previous_response_id is only supported on Responses WebSocket v2`。这是调用链协议状态不一致：HTTP continuation 缺少工具调用对应的 `call_id`，却尝试只靠 `previous_response_id` 继续。它不是 MotoMate 业务代码故障，但会造成任务看似接续、实际无法执行。

**解决方案与验证**
不对中转站做高风险修改。发生后先保留工作区，重启任务或新建任务继续，并以 Git 状态确认没有丢失文件。后续若重复出现，应记录客户端版本、传输模式、请求序列和 operation/thread ID，再单独修复协议适配层。

**可迁移经验 / 面试表达**
工具型 Agent 的错误需区分业务层、编排层和传输协议层；不能为了修协议错误去改业务产品。当前方案恢复了工作，但仍属于运维降级，不是根治。

---

## MPS-020：历史需求覆盖当前问题，导致重复推荐

**案例编号、日期/阶段、状态**
MPS-020；2026-09-01；已解决。

**问题、根因与影响**
在已有预算和推荐上下文中，用户问“我还没骑过车怎么办”，系统却重复上一轮推荐过渡。根因是意图路由优先读取历史结构化 `needs`，没有先判断当前消息的显式意图，导致 Agent 看起来只能执行固定流程。

**解决方案与验证**
`alpha-orchestrator.js` 改为当前轮显式意图优先，历史需求只作回退上下文；补充“没骑过、不会骑、零基础、第一次骑、新手怎么办、怎么学骑”等识别。真实 API 返回 `motorcycle_general -> show_open_answer`，API、20 条冒烟和 50 条黄金评测通过。

**可迁移经验 / 面试表达**
多轮 Agent 中“记得过去”和“听见现在”必须分开设计，当前轮显式意图的优先级通常高于历史槽位。

---

## MPS-021：记忆政策、工程实现和隐私告知冲突

**案例编号、日期/阶段、状态**
MPS-021；2026-09-01；已解决当前内部 Alpha 范围，正式公网仍需认证、云存储和安全评审。

**问题、根因与影响**
旧文档同时存在“最近 6 条消息”“授权后保存完整会话 30 天”“登录不等于保存授权”，而新决策是约 20 轮短期上下文、24 小时删除、白名单长期偏好和匿名设备 ID。若只改代码不改告知，会出现用户被告知“不保存原文”，系统却临时保存对话的合规问题。

**解决方案与验证**
新增短期消息、滚动摘要、长期偏好和跨会话观察存储；摘要脱敏手机号、邮箱、证件号和车牌。长期仅允许 6 类白名单字段，明确“以后都……”可立即晋升，否则需跨会话重复；本次需求始终优先。同步 PRD、架构、System Prompt、测试主持词和就绪指标，并在 UI 提供首次告知、开关、查看、编辑、删除和清空。记忆测试 18 场景、API 25 场景通过。

**可迁移经验 / 面试表达**
记忆不是给模型多塞历史，而是一个有身份、写入门槛、优先级、生命周期和删除权的数据产品。

---

## MPS-022：年轻化回复放松了新手安全边界

**案例编号、日期/阶段、状态**
MPS-022；2026-09-01；已解决。

**问题、根因与影响**
为减少 AI 味，提示词改成“年轻懂车朋友”后，真实模型回答更自然，但出现“找空旷场地练”“踏板车重心低、对新手友好”等泛化表述。长期踏板偏好还被带入安全问答，说明风格优化可能无意中削弱事实与安全门禁。

**解决方案与验证**
对话改为短句、少复述、禁用 AI/客服套话，同时明确不玩梗、不装熟。新手学习问题必须先提示正规培训、驾驶资格和合法有指导的封闭场地；不得把小排量、低座高或车型类别直接等同于适合新手。增加确定性输出拦截和安全短回答，开放问答测试扩至 8 场景，Agent 测试、20 条冒烟和 50 条黄金评测通过。

**可迁移经验 / 面试表达**
语气指标和安全指标必须分开评测。更像人不代表更可信，真实模型试跑比只检查提示词更容易发现风格改动带来的行为回归。

---

## 2. 当前自查记录

**术语自查**  
已统一使用 `extraction_draft`、`fact_verification_status`、`rule_pool_eligibility`、`recommendation_review_status`、`eligible`、`approved`、`Codex复核`、`中性缺失`。本文未把 Codex 复核写成人工专家审核，也未把 Ethan 的产品策略批准写成逐字段事实审核。

**状态自查**  
当前共 22 个案例：已解决 9 个，部分解决 13 个，待验证 0 个，开放 0 个。部分解决案例主要是产品策略或局部工程已明确，但仍缺生产监控、用户验证、正式推荐批准、协议层根治或外部平台状态核验。

**事实边界自查**  
已明确标注：首轮用户测试尚未执行；通用价格刷新策略尚未实现；新文档已合并到主项目但尚未提交 GitHub；GitHub 账号迁移过程缺少仓库内证据；智谱权益到期日和每日重置规则未由本任务独立核验；语义增强和 `verified_temporary_candidate` 均不构成 recommendation approval；当前 17 配置只用于内部 Alpha 链路验证，不证明车型覆盖充分；50/50 黄金评测不代表公网发布就绪。

**文件链接自查**  
本文引用均为仓库相对路径，未引用外部私密链接，未记录账号邮箱、验证码、API Key、Token 或其他凭据。

**本次验证命令结果**  
- `node scripts/validate-eligible-pool-v02.mjs knowledge_base_outputs/eligible_pool/first_batch_v1.json knowledge_base_outputs/eligible_pool/manifest.json knowledge_base_outputs/exceptions/first_batch_closure_exceptions.json`：通过，8 条有效，eligible 8，approved 0，errors 为空。
- `node scripts/validate-semantic-enrichment-v01.mjs knowledge_base_outputs/semantic_enrichment/first_batch_semantic_v0.1.json knowledge_base_outputs/eligible_pool/first_batch_v1.json knowledge_base_outputs/semantic_enrichment/manifest.json knowledge_base_outputs/semantic_enrichment/first_batch_semantic_exceptions.json`：通过，8 条有效，maintenance 可排序 0，approved 0，模型调用 0。
- `node scripts/test-rules-engine.mjs`：通过，7 个场景。

**仍缺证据的案例**  
- MPS-001：缺首轮 10 至 20 位用户测试结果。
- MPS-005：缺 `cache-first / stale-while-revalidate` 工程实现与线上刷新日志。
- MPS-010：缺 Agent 架构文档合并、PRD/System Prompt 同步和网页原型接入真实推荐链路后的验证。
- MPS-011：缺编排器、模块化工具、20 条工程冒烟和 50 条黄金集发布门控的工程实现。
- MPS-012：缺 GitHub 账号迁移过程和私有仓库外部平台状态的可审计证据。
- MPS-013：缺智谱权益到期日、每日重置规则和实际抵扣归属的平台账单/权益明细核验证据。
- MPS-014：缺 GitHub 同步、20 条冒烟评测建立和 8 配置内部 Alpha 运行结果。
- MPS-015：缺生产环境搜索稳定性、来源失效、时延与费用监控。
- MPS-017：缺真实批准批次，以及认证、云数据库与备份、用户删除、邀请码与容量、每日限额、真实费用计量和管理接口保护。
