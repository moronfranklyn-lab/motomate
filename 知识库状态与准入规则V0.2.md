# 知识库状态与准入规则 V0.2

状态：当前工作约定草案，替代整车级 `field_verified / pending_verification / recommendation_ready` 混合语义。旧文件保留为历史证据，不按旧目录名推断当前状态。

## 1. 三个正交维度

### 1.1 fact_verification_status

只回答“事实字段由什么等级的证据支持”，不回答车型是否可筛选或可推荐。

```json
{
  "overall": "not_started|in_progress|sufficient_for_rules|conflict|stale|insufficient",
  "fields": {
    "sale_status": "official_verified|provisional_verified|unverified|conflict|stale|not_applicable"
  }
}
```

- `official_verified`：字段由品牌官网、官方商城、官方现行目录或明确官方材料支持。
- `provisional_verified`：仅适用于允许降级的参数字段；必须有两个相互独立的主流平台一致支持且无冲突。
- `unverified`：没有达到上述证据门槛，值应为 `null` 或只留观察值。
- `conflict`：同配置、同口径证据冲突。
- `stale`：证据超过有效期。
- `sufficient_for_rules` 只表示硬筛选所需字段达到门槛，不暗示完成推荐复核。

### 1.2 rule_pool_eligibility

只回答“能否参加确定性准入与排序规则”。

```json
{
  "status": "not_evaluated|pending_deterministic_check|eligible|blocked|ineligible|excluded",
  "blockers": [],
  "rule_version": "kb_admission_v0.2",
  "evaluated_at": null
}
```

- `eligible` 只允许进入确定性筛选池，不等于可以对外推荐。
- 价格或在售冲突必须为 `blocked`。
- 不满足品牌、官方价格区间或当前在售要求为 `ineligible/excluded`。
- 参数缺失只在该字段参与硬规则时阻塞；不能用其他维度分数抵消价格和在售门槛。

### 1.3 recommendation_review_status

只回答“推荐层是否完成复核”。

```json
{
  "status": "not_started|codex_review_pending|codex_reviewed|ethan_decision_required|approved|rejected",
  "review_notes": [],
  "reviewed_at": null
}
```

- Codex复核必须记录为 `codex_reviewed`，不得称为人工核验。
- 只有 `rule_pool_eligibility.status=eligible` 且 `recommendation_review_status.status=approved` 才能进入最终推荐池。
- 模型抽取通过、字段事实通过或Codex复核，任何一个单独事件都不能自动导出 `approved`。

## 2. 最小可信准入标准

1. 在售状态必须由品牌官网、官方商城或官方现行目录确认，禁止以平台在产状态替代。
2. 官网未明确年份时，`model_year` 使用 `current_version`；平台年份可作为观察信息，不得伪造成官方年款。
3. 官网明确展示“售价”时写入 `official_public_price_cny`，`official_price_label=官网售价`；不得改写成指导价或建议零售价。
4. `official_public_price_cny` 必须在10000至30000元；在售状态和价格不允许使用 `provisional_verified`。
5. 主体、车型、配置必须唯一绑定；不同合资主体和不同配置建立独立 `model_id`。
6. 排量、座高、整备质量、功率、油箱、ABS、TCS优先使用官方证据。官方缺失时，两个独立主流平台一致且无冲突方可标 `provisional_verified`；单平台值只能作为观察值。
7. 在售或官方价格冲突阻塞整车进入规则池。参数冲突只阻塞依赖该参数的规则，并显式降级。
8. 主观口碑、二手行情与向量证据不属于本轮裸车硬准入字段，不得影响核心准入和预算排序。

### 2.1 价格策略 A

1. 官网公开价格保存为`official_public_price_cny`，并保留官网原始口径标签。
2. 平台价只有同时满足以下条件才写入`qualified_platform_reference_price_cny`：同年款或同一`current_version`、同配置、同价格口径、核验时间在24小时内、来源为允许的主流平台。
3. 预算准入使用`budget_guard_price_cny = max(official_public_price_cny, qualified_platform_reference_price_cny)`；没有合格平台参考价时等于官网公开价。
4. 低于官网价的平台观察值只展示为“可能优惠/市场参考”，必须显示平台、核验时间，并提示“不是成交价，需向门店确认”。
5. 配置、年款或价格口径无法匹配的平台价不得参与预算筛选，只能写入`platform_reference_price_cny`和备注。
6. 官网价与平台价差异先分类为`explainable_market_difference`、`configuration_or_scope_unclear`或`true_same_scope_conflict`。
7. 价格差异本身不再一律产生`price_conflict`；只有同年款/当前版本、同配置、同口径仍矛盾的`true_same_scope_conflict`必须阻塞整车。

### 2.2 按需价格刷新策略（本地解析层已实现，联网刷新尚未实现）

1. 用户询价采用`cache-first / stale-while-revalidate`：缓存有效时直接返回；缓存过期时展示旧值、价格状态和最后更新时间，同时在后台刷新。
2. 没有任何缓存时才展示核验进度；刷新目标20秒完成，30秒强制超时。超时或失败不得阻塞旧值展示。
3. 抓取优先级为官方公开API、官方页面、主流平台补充。官网价与平台价分别保存，任何来源不得覆盖另一来源的历史记录。
4. 新价格只有在车型、`current_version`/明确年款、具体配置和价格口径全部匹配，并通过确定性校验后，才能追加到价格历史并重新计算`budget_guard_price_cny`。
5. 刷新失败时保留最近旧值，字段标记`stale`并记录失败原因；无缓存时明确告知暂无法核验。禁止使用模型记忆、语言模型推断或无来源数值补价。
6. 每条价格历史至少保存：`source_url`、`source_name`、`captured_at`、`valid_until`、`configuration_match`、`verification_status`、价格字段、价格口径、车型/版本/配置标识和刷新结果。
7. 网页内容一律视为不可信输入：只接受来源白名单，网页正文与系统提示隔离，忽略页面中的指令性文本；实施域名级限流、超时、失败熔断，并遵守站点robots、服务条款及访问规则。
8. `stale-while-revalidate`只影响展示和刷新流程，不降低规则池门槛；真正同口径冲突、配置不匹配或来源校验失败仍按V0.2状态规则处理。

## 3. 低人工复核流程

1. 确定性脚本全量检查：Schema、字段状态枚举、来源URL、来源独立性、官方在售/价格、价格口径、配置绑定、冲突、有效期与准入规则。
2. Codex复核所有异常，以及所有准备进入最终推荐池的记录。该步骤称“Codex复核”，不称人工核验。
3. Ethan只复核：政策变更、官方价格/在售冲突、每批10%确定性随机样本。
4. 抽样记录批次ID、随机种子、总体数量、样本ID和抽样时间；不足10条至少抽1条。
5. Ethan不逐条复核正常字段；未出现在审核清单中的正常记录默认继续机器流程，但仍不得自动进入最终推荐池。

## 4. 目录语义

```text
knowledge_base_outputs/
  extraction_drafts/  # 模型原始草稿，仅用于抽取链路审计
  verified_fields/    # 字段级证据与状态，不表示整车可推荐
  eligible_pool/      # 仅放通过确定性准入的记录
  exceptions/         # 冲突、缺失、排除、待决策和迁移映射
  route_validation/   # 只证明抽取路由表现
  legacy_manifest/    # 旧路径到新语义的映射，旧文件不删除
```

旧 `knowledge_base_outputs/verified/` 为历史目录。文件名或所在目录不再作为当前状态依据，当前状态以V0.2 manifest为准。

## 5. 禁止推导

- `extraction_route_passed` 不推出任何事实已核验。
- `official_verified` 字段不推出整车 `eligible`。
- `eligible` 不推出 `recommendation_review_status=approved`。
- `codex_reviewed` 不等于 Ethan 已审核。
- `current_version` 不得反向改写为具体年份。
