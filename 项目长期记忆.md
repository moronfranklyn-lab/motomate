# 项目长期记忆

## 2026-08-29

- Ethan 正在系统学习 AI 产品经理方法。此后项目每推进并完成一个重要阶段，主任务需增加一次资深 AI 产品经理视角的教学复盘，至少解释：阶段目标与用户价值、范围和优先级判断、关键产品/技术取舍、验收与风险、可迁移的方法，以及该阶段在面试作品中的叙事方式。日常小改动不机械展开，避免打断执行节奏；重要阶段完成时再集中讲解。
- 协作澄清规则：当主任务对 Ethan 的真实目标、范围、验收标准或执行方式未达到 95% 理解把握时，必须先暂停执行并询问；每次只问一个最关键问题，提供 2 至 3 个互斥选项、推荐项及取舍说明。不得自行补齐关键产品决策，也不得因 Ethan 的表述方式而迎合未经核查的前提。
- 智谱资源包最新口径：Ethan 于 2026-08-29 反馈当前权益还剩约 6 天且资源包每日重置；该期限与重置规则来自 Ethan 账户使用反馈，所附截图未显示到期日或重置规则，尚未独立核验。截图可确认当时资源包均为生效中，页面显示 `glm-4.7` 约 5,000,000 tokens、`glm-4.6v` 约 6,000,000 tokens、`glm-4.5-air` 约 11,978,637 tokens、通用按 Token 资源约 1,785,656 tokens，另有约 86 次搜索和 20 次按次调用。后续资料采集、证据整理、批量抽取、评测初筛等高 Token 窄任务可优先调用本机智谱 API，但不得为了消耗额度扩大 MVP 范围；仍需遵守来源核验、结构化校验、usage 日志、单请求上限、失败熔断及密钥只保存在本机环境变量的规则。模型选择继续按已测任务表现决定，不因余额较多而降低质量门槛。
- Agent 需求与架构设计采用分阶段确认：每阶段先汇报结论，再只问 Ethan 一个关键问题，收到确认后才进入下一阶段；11 个阶段现均为 `Ethan_confirmed`，但设计确认不表示功能已实现。
- 内部 Alpha 允许使用当前 8 个 `rule_pool_eligibility=eligible`、尚未 recommendation approved 的配置做受控 `development_preview`，只用于验证端到端链路，不得对外宣称正式推荐。链路稳定后扩至 12 至 15 款验证覆盖度；外部 MVP 仍以约 25 至 35 款且完成正式推荐批准为门槛。
- 意图路由采用主意图优先：一句话包含多个需求时，先处理最核心的选车、车型判断或对比任务，其他需求作为后续入口；MVP 不默认多意图并行。
- 新手推荐前的最低信息门槛为预算、主要用途、新车/二手偏好。车型方向不是必填项；新车/二手偏好缺失时必须追问，不由系统静默默认。
- 硬规则筛选无完全匹配时，可以单独展示规则池内的最接近候选，但必须逐项标明未满足条件并说明不是正式推荐。系统不得自动放宽条件；只有用户明确同意后才按新条件重新执行。
- 用户实时推荐流程采用一个编排器调用模块化工具，禁止启动独立 Agent。独立 Agent 仅用于离线知识库扩建、批量评测和失败归因；联网价格、证据核验等窄工具调用可在可合并且能降低延迟时受控并行。
- 知识库缺失值分级处理：在售状态、价格、推荐资格等硬字段缺失时不得进入正式候选；语义排序字段缺失时保持中性，不由 AI 插补，不加分也不扣分，并保留缺失原因、来源状态和数据版本。
- 新车价格继续使用策略 A：官网公开价格为基础；存在同版本、同配置、同口径且 24 小时内核验的合格平台参考价时，`budget_guard_price_cny` 取二者较高值，较低促销价只作参考。二手车使用独立车源报价与风险核查逻辑，不直接套用新车官方售价准入。
- 首轮 10 至 20 人测试只上线轻量二手文字助手，不开放图片分析。图片能力推迟至首轮测试后，只有核心价值验证通过且完成视觉模型专项评测、图片授权、自动脱敏、供应商条款和删除链路验收后才可开放。该决定覆盖 PRD 中图片首发的旧设想，PRD 尚待同步。
- 登录不等于同意长期保存购车记忆。匿名用户和未主动保存的登录用户，其结构化状态只用于当前会话；用户主动保存后，确认的结构化购车记录才进入账户并适用查看、编辑、删除及连续 180 天未登录清理规则。
- 评测采用分层运行：20 条工程冒烟在代码、Prompt、规则或知识库变更后自动运行；50 条黄金评测在上线前及模型、规则或推荐策略发生实质变化时运行并作为发布门槛。首轮不启用图片，因此相关冒烟案例改为二手文字信息不足和事故、泡水、调表越界鉴定测试。

## 2026-08-27

- 项目第一目标确认为 AI 产品经理面试作品，同时要求具备真实上线能力；若验证出商业价值，Ethan 愿意继续投入和深挖。面试展示重点应包括用户研究、需求取舍、Agent 设计、知识库与数据可信度、评测体系、风险边界、迭代证据和商业路径，而不只是界面或代码。
- 产品统一主叙事确认为“新手选车推荐是核心流程，轻量二手车助手是辅助亮点”。首页、PRD、评测与面试讲述均按此优先级组织，不将两条流程做成同等权重。
- 核心新手选车流程同时覆盖新车和二手车型选择：先识别用户偏好；用户未明确时以新车推荐为主，在预算或车型价值确有优势时提示合适的二手选择。具体二手车源的信息与风险分析仍由轻量二手助手承担。
- MVP 的二手车型推荐仅覆盖首批知识库 25 至 35 款车型最近 3 年的二手版本，不扩展到所有品牌和历史车型。年份以首次登记日期或可核验年款为准；年份无法确认的具体车源只进入风险助手，不作为可信推荐样本。
- 首轮上线验证目标为 10 至 20 位目标用户，分两批进行：先用前 5 位开展深度可用性测试并修正明显问题，再邀请其余用户验证推荐价值和使用意愿。
- 测试用户采用混合招募：前 5 位从朋友、同学或摩友圈中选择符合目标画像的人，便于观察和回访；迭代后通过小红书、贴吧、摩托群等渠道招募陌生目标用户。分析时区分熟人与陌生样本，控制礼貌性反馈偏差。
- 首轮验证主指标为“推荐结果是否帮助用户做出购车决策”。除量化评分外，还需收集用户能否说明具体帮助：缩小候选范围、理解预算取舍、排除不适合车型或明确下一步核验事项。流程完成率、保存意愿和再次使用意愿作为辅助指标。
- 单个用户被判定为“推荐有帮助”必须同时满足：5 分制评分不低于 4 分；用户能够说出至少一项具体帮助。只有高分或泛泛的正面评价不计为有效成功样本。
- 推荐反馈采用 5 分帮助度评分、具体帮助标签和一句简短说明。帮助标签至少包括缩小候选范围、理解预算取舍、排除不适合车型、明确下一步及其他；评分和具体说明共同用于成功判定。评分高低不影响邀请码资格。
- 用户评分 1 至 3 分时，收集失败原因标签并允许选填补充说明。原因标签包括推荐超预算、不符合用途、不喜欢车型或外观、参数或价格可能错误、追问太多、解释看不懂、缺少目标品牌/车型和其他。
- 行为数据只记录核心漏斗事件：邀请码兑换、开始咨询、完成需求识别、生成推荐、查看依据、开始/完成二手分析、保存记录、提交反馈、按新偏好重新推荐、生成邀请码和兑换邀请码。事件仅携带匿名会话 ID、登录后的用户 ID、时间、场景和结果状态，不记录输入原文、鼠标轨迹等无关行为。
- MVP 暂不提供独立的车型参数/价格纠错及来源提交入口；仅通过低评分中的“参数或价格可能错误”标签收集线索，后台仍需自行核验后更新。
- 首轮 MVP 初步验证成功门槛为至少 70% 测试用户满足单用户成功口径。熟人与陌生用户需分别统计；10 至 20 人的小样本结果仅用于阶段性迭代决策，不推断整体市场。
- 上线前建立 50 条离线黄金评测集，覆盖核心选车、信息缺失、需求冲突、超范围、价格冲突、车型对比和二手风险等场景。每条案例需包含输入、预期结果与判定依据；提示词、规则或知识库变更后运行回归评测。
- 50 条评测集分布确认为：新手选车正常场景 20 条，信息缺失与追问 8 条，需求冲突/无匹配/超范围 7 条，指定车型判断与多车对比 5 条，价格过期/来源冲突/核验失败 5 条，二手车源风险 5 条。
- 上线前离线评测门槛：预算、在售状态等硬约束违规为 0；车型、价格和参数事实准确率不低于 95%；需求字段提取准确率不低于 90%；应追问/不应追问判断准确率不低于 90%；正常案例推荐前追问不超过 3 次；二手助手越界鉴定事故、泡水或调表为 0。未达门槛时修正后运行完整回归。
- 评测判定采用混合方式：程序自动检查硬约束与结构化字段，AI 初评解释质量，Ethan 人工复核全部失败案例并抽查成功案例。AI 评分不得作为唯一上线依据。
- 人工评测抽样：全部失败案例必须复核；成功案例按场景分层抽查 20%（50 条评测集中约 10 条）。发现 AI 误判时，扩大对应场景的抽查比例。
- 核心推荐采用“规则筛选 + AI 解释”的混合方案：规则根据预算、用途、坐高、经验等生成可审计候选池；AI 负责理解自然语言、低打扰追问、取舍比较和个性化解释。AI 不得绕过候选池凭空推荐车型。
- 当用户条件超出首批知识库范围时，允许系统联网搜索临时候选车型，但搜索结果不能直接成为推荐。临时候选必须经过来源、在售状态、价格和关键参数核验，并在结果中标注外部来源与核验时间；无法完成核验时只提供选车原则，不推荐具体车型。
- 临时候选进入推荐的最低来源要求：品牌官网确认在售状态和关键参数，并由至少一个主流车型平台交叉核对价格。系统提示词只负责约束核验流程、引用和不确定性表达，不能单独证明事实准确；实际核验依赖可访问的数据源、检索工具、结构化校验与失败降级。
- 对同一年款、同一配置且同一价格口径的数据，两个来源价格差异超过 10% 时标记为价格冲突。该车型不得成为首选，但可作为单独的“待核价备选”展示；必须同时显示两个价格、来源、核验时间和冲突提示，并进入后台待核查列表。官方指导价与门店优惠价、裸车价与落地价等不同口径不得直接计算差异。
- “待核价备选”不占用最终 1 至 3 款正式推荐名额，只能在正式推荐结果之后独立展示；正式推荐车型必须全部通过数据核验。
- 价格采用“知识库缓存 + 过期后按需联网核验”：官方指导价有效期 7 天，平台新车参考价和二手挂牌参考价有效期均为 24 小时。推荐结果必须展示价格类型、来源和最后核验时间；二手挂牌价不得表述为成交价。
- 首发产品方向优先选择「新手选车顾问」，而不是直接做二手车源交易平台。
- 核心用户先聚焦初次购买摩托车的小白用户，帮助他们理解预算、用途、车型、身高/骑行经验、购车总成本和风险取舍。
- 后续二手车交易、车商渠道、检测/陪买服务可以作为第二阶段能力接入，但首版产品应先建立选车决策信任。
- Ethan 选择先产出「MVP产品需求文档」；飞书问卷链接作为参考资料，但当前环境无法直接抓取飞书 Base 页面内容，后续若要分析真实问卷结果，需要导出表格或提供可访问数据。
- 首发形态确认为网页应用。此前误生成的静态网页原型文件暂时保留，但在需求确认完成前不继续开发；原型内容不视为已确认需求。
- MVP 允许用户先匿名体验；只有在用户主动保存购车需求或推荐记录时，才引导其登录。
- MVP 登录方式确认为用户名和密码。匿名用户点击保存时进入注册或登录流程；密码必须使用成熟密码哈希算法保存，不得明文存储，并需具备重复用户名校验、登录限流和基础失败保护。忘记密码时使用注册阶段生成的恢复码：恢复码仅向用户展示一次，服务端只保存哈希，使用后立即作废并生成新码。
- MVP 保存用户确认后的完整购车记录，包括需求画像、推荐车型、对比结果和反馈；默认不将全部聊天原文作为永久档案。用户应能查看和删除记录，向人工顾问或车商共享前必须再次授权。
- 用户连续 180 天未登录后，自动删除其保存的购车记录。由于 MVP 不收集邮箱或手机号，不提供站外到期提醒；登录后需展示数据保留规则和预计清理时间。
- 用户单独授权后可保存完整文字会话 30 天用于产品评测，到期自动删除；结构化购车记录仍按连续 180 天未登录自动清理。二手原图继续在当次分析完成或终止后删除。
- 管理员默认只查看脱敏会话摘要。查看完整原文必须满足用户已授权且仍在 30 天期限内，管理员需填写复盘原因，并记录查看人、时间和会话 ID；到期或用户删除后不得继续访问。
- 为减少 Token 消耗，模型默认读取结构化用户记忆和少量最近对话，不读取完整历史。结构化记忆仅保存与购车直接相关且由用户提供的信息，如预算、用途、身高、经验、新车/二手偏好、车型偏好、明确排斥项和当前候选；不推断或保存无关敏感画像。
- 每次模型调用默认携带结构化记忆、最近 6 条消息和当前问题。引用更早内容时只检索相关历史片段，不加载完整会话；记录每次调用的输入 Token 用于成本评估。
- 用户明确表达的购车信息可直接写入结构化记忆；AI 推断的偏好必须先由用户确认。每条记忆保存来源、更新时间及“用户明确/AI 推断后确认”类型。
- 用户可在“我的购车偏好”中查看、编辑和删除结构化记忆。删除后该字段不得继续进入模型上下文或影响后续推荐。
- 修改购车偏好只影响未来推荐，不改写历史结果。用户可点击“按新偏好重新推荐”创建新版本；新旧版本分别保留当时的需求、数据核验时间、规则版本及推荐结果。
- 用户可在账户设置中自助注销；注销前需重新输入密码或恢复码确认，注销后删除账户及全部关联购车记录，且不可恢复。
- 数据库策略：开发阶段可使用本机数据库；正式上线供多人使用前迁移至云端数据库，不要求当前立即租赁服务器。
- 首批车型知识库的价格范围确认为裸车价 1 万至 3 万元。范围外需求可以识别，但 MVP 不承诺完整车型覆盖和推荐。
- 已读取 `访谈记录整理.md`：共 6 条有效访谈，样本量小且部分字段缺失。6 人均直接或间接提到价格；明确出现的车辆为奔达灰石 250、春风 500SR、春风 450CLC。该材料可支持价格透明、真实体验、品控与信息可信度是重点，但不能单独用于推断整体市场的品牌偏好或确定完整品牌清单。
- 车型资料来源策略：品牌官网用于核实在售状态和官方指导价；摩托范、汽车之家摩托车频道用于建立候选池和交叉核验，平台报价不直接等同于门店成交价。车型记录需区分官方指导价、平台参考价和核验日期。
- 品控、常见争议和真实体验等主观信息，至少需要一个专业评测和三条相互独立的车主讨论支持，并记录对应年款与来源。只能表述为“部分车主重复反馈”或“建议试驾/核查”，不得据此断言车型必然存在质量问题。
- 推荐结果首屏不直接铺开来源链接；用户点击“查看依据”后，展示主要价格、参数和争议反馈的来源、对应年款及最后核验时间。后台保存完整证据链。
- 性能目标：2 秒内显示需求摘要或处理进度；缓存命中的普通问诊目标 10 秒内完成；需要联网核验的完整推荐目标 20 秒内完成；30 秒强制超时并说明失败环节、允许重试。二手图片分析单独设置性能指标。
- 系统主框架采用一个编排器和模块化工具；用户实时推荐流程不启动独立 Agent。预算筛选、准入条件、权重排序和结构化校验优先使用确定性程序规则。
- Agent 使用动态路由：编排器根据用户意图、缓存状态和输入类型，只调用必要的需求提取、数据核验或解释工具；普通追问不启动全部工具，缓存有效时不重复联网核验。
- 图片分析目标 30 秒、60 秒强停，以及图片数量、压缩、脱敏、外部视觉模型和单独授权要求，均保留为首轮测试后的预留设计，首轮文字助手不启用这些能力。
- 首轮上线采用邀请码控制访问。每个注册成功的用户可获得 2 个可追踪的一次性邀请码邀请朋友，用于控制测试规模和区分招募来源，不读取或上传用户通讯录。管理员可暂停发放、作废未使用邀请码或按测试容量补发。
- 用户完成至少一次推荐、提交反馈并注册保存后，解锁“生成邀请码”按钮；用户主动点击生成，最多 2 个。反馈内容不影响发码资格，不得要求正面评价。
- 邀请码生成后 7 天过期；过期未使用时自动失效并释放对应额度，用户可重新生成。每位用户最多成功邀请 2 人。
- 首轮测试注册用户全局上限为 20 人。达到上限后暂停邀请码兑换和新邀请码生成；已生成邀请码保留剩余有效期，名额重新开放后可继续使用。界面需提示“测试名额已满”，不得误报邀请码错误。
- 每位用户每天最多使用 5 次完整选车咨询和 2 次二手图片分析。同一次咨询内的必要追问不重复计次；达到限额后仍可查看历史记录和已有结果，不再产生新的对应模型调用。
- 首轮 20 人测试的模型费用硬上限为 50 元。系统需分别记录文本、图片及相关模型调用成本；达到上限后自动阻止新的付费模型请求并进入基础模式：保留规则筛选和固定模板解释，暂停自由对话、联网临时候选解释和二手图片分析。界面必须明确提示降级状态。
- 部署策略：先在本地完成开发，再核查并选择具备免费额度的前端托管、后端和数据库服务上线。当前不锁定具体平台；上线前实测中国大陆访问速度、免费额度、休眠策略、数据保留和迁移能力，不满足要求时再评估腾讯云。
- 网页版采用同一套响应式 MVP 同步适配手机端和桌面端，不拆成两套业务逻辑。手机端优先保障真实用户测试体验，核心问诊、推荐、图片上传、反馈和保存流程按手机单手操作设计；桌面端同步保证功能完整，并强化面试展示、车型对比、推荐依据、知识库后台和评测结果的可读性。
- 首轮兼容微信内置浏览器、手机 Chrome、iPhone Safari 和桌面 Chrome 的当前主流版本。上线测试覆盖登录、图片选择与上传、恢复码保存、推荐结果和历史记录。
- 首页以自然语言输入框为主，提供预算、用途和车型偏好等快捷选项辅助。快捷选项不构成必填表单；信息足够时直接推荐，信息不足时每轮只追问一个关键问题。
- 首批车型知识库确认为 8 个品牌、约 25 至 35 款在售车型：春风、QJMOTOR、无极、豪爵、奔达、升仕、本田系合资品牌、雅马哈系合资品牌。车型还须满足裸车指导价 1 万至 3 万元、当前在售且资料可核验；品牌旗下车型不自动全部纳入。
- MVP 暂不提供推荐结果分享链接或分享海报。分享能力保留为后续版本候选，不属于当前验收范围。
- MVP 暂不接入人工顾问，只提供“结果不满意、信息有误”等反馈入口。人工顾问、车商承接和陪买服务属于后续阶段。
- 首发包含最小运营后台：车型与证据来源维护、价格冲突队列、用户反馈、脱敏会话复盘、邀请码与 20 人容量管理、模型费用和调用量监控。首轮仅一个管理员账户，不建设复杂角色权限、车商或渠道后台。
- 系统先识别用户预算是裸车预算还是购车总预算；无法判断时只追问一次，让用户选择“裸车预算、购车总预算、不清楚需系统估算”。裸车预算需单独展示预计额外支出；购车总预算需扣除税费、保险、上牌和基础护具预留后筛选车型；不清楚时使用可调整区间估算。
- 推荐预算规则：首选车型的预计落地总成本不得超过用户总预算；备选最多可超出总预算 5%，并必须展示超出金额及推荐理由。超过 5% 的车型不得进入推荐结果。
- 购车总成本估算属于首发 P0。系统分别估算购置相关税费、保险、上牌和基础安全装备，并展示预算拆分；用户可修改预留金额，推荐引擎按调整后的可用裸车预算筛选。不得把估算结果表述为精确落地价。
- 首年保养、停车和通勤费用不占用购车总预算，单独归入“首年持有成本”展示。购车预算用于筛选当前可购买车型，首年持有成本只用于提醒后续养车压力。
- 候选排序采用“基础权重 + 用户明确偏好动态调整”。预算、在售状态和数据可信度属于准入条件，不能用其他维度高分抵消；AI 从用户表达中提取偏好优先级，规则引擎完成可解释排序。
- MVP 初始排序权重确认为：用途匹配 25 分、人体适配 15 分、新手友好与安全 20 分、总成本余量 15 分、品控/售后/维护便利性 10 分、外观/动力等个人偏好 15 分。用户明确表示不喜欢外观的车型不得作为首选；明确强调颜值或动力时，个人偏好可动态提高至最高 25 分，并从其他软性维度等比例扣减，总分保持 100。该权重是待用户测试验证的产品假设，不作为市场事实。
- 100 分匹配分仅用于内部排序与评测，不向用户展示具体总分。用户端显示首选/次选/备选、主要匹配理由、妥协点和排序依据，避免伪精确感。
- 单次最多对比 3 款车型。用户输入超过 3 款时，系统根据已确认需求筛选出最值得比较的 3 款，并说明其余车型暂时排除的原因。
- 身高不作为车型硬性淘汰条件，但必须参与风险提示。系统结合座高、整备质量和骑行经验说明可能的落脚及挪车压力，并建议实际试坐；不得只凭身高断言用户一定能骑或不能骑。
- 仅当身高适配可能改变推荐结果时，追问腿长、落脚习惯或类似车型试坐经历；该问题选填且允许跳过，不作为所有用户的固定问卷项。
- 完全无骑行经验的用户偏好高动力车型时，不直接否定，但高动力车型不得作为默认首选；系统说明油门响应、车重、制动和低速操控压力，并提供更温和替代。用户坚持时可作为偏好备选，不使用保证性表达。
- 每款车型维护低/中/高三级“操作压力”综合标签，依据功率、整备质量、油门特性、座高、低速操控和经验要求计算后人工核验。AI 仅解释标签依据，不得临时改写标签。

## 2026-08-28

- 所在城市为选填项。仅当用户需要落地预算、上牌或禁限摩信息时询问，收集粒度限制在城市级别，不索取详细地址。政策提示需标注核验日期和官方来源，不作为确定的法律承诺。
- MVP 不自动总结或解释城市禁限摩、上牌政策，只提示用户需要核查，并提供当地交管部门等官方查询入口及链接核验时间；第三方文章不得作为正式政策结论。后续若测试用户集中在少数城市，再评估试点政策摘要。
- 摩托车驾驶证状态为选填项，在涉及试驾、排量或实际上路建议时用于安全与合规提醒，不作为获得车型推荐的门槛。未取得相应驾驶资格时，系统必须提示不得上路。
- 网页版 MVP 首发包含轻量二手车助手。用户可提交车源信息和价格，系统只输出信息缺口、风险点、询问卖家的问题与线下验车清单；不鉴定事故车、泡水车或调表车，不替代线下检测，不直接撮合交易。
- 轻量二手车助手支持提交车源文字、挂牌截图和车辆照片。图片仅用于提取车源信息、发现信息缺口和提示需要线下核查的位置，不得据图判定事故或泡水。车牌、卖家账号、联系方式和人脸按敏感信息处理。
- 二手车图片默认仅用于当次分析，分析完成后删除原图，只保存必要的结构化结果；结构化结果不得保留完整手机号、车牌号、卖家账号或人脸等不必要的敏感信息。
- 知识库建设允许引入豆包 API 辅助，但只作为公开车型资料的信息收集与初步结构化助手，不作为事实来源。豆包输出必须标记为待核验草稿，经字段清洗、价格口径区分、来源链记录、核验时间、价格冲突判断、数据状态和人工复核后，才可进入可信结构化知识库。
- 豆包 API Key 不得出现在聊天、文档、代码提交或长期记忆正文中；后续调用只能通过本机环境变量或本地 `.env` 读取，例如 `DOUBAO_API_KEY`。示例配置只能写 `.env.example`，不得写真实密钥。
- 豆包 API 首批只处理公开车型资料，不传用户隐私、二手图片、账号信息、聊天原文或任何敏感数据。二手图片与真实用户数据的外部模型处理需等隐私条款、用户授权和脱敏链路单独确认后再接入。
- 向量化证据库只用于证据召回、解释和“查看依据”，不得参与核心准入、预算筛选或排序；核心推荐仍由结构化知识库和确定性规则控制。
- 智谱 API 已在本机项目 `.env` 中配置并完成连通性测试。`glm-5.3` 已完成可用文本返回验收：普通文本和 JSON 结构化输出请求均返回 HTTP 200，文本非空，JSON 可被标准解析器成功解析。该模型强制思考，可用参数为 `thinking: { type: "enabled", effort: "low" }`，不支持关闭思考。`glm-4.7`、`glm-4.6v`、`glm-4.5-air` 也均已返回 HTTP 200、正常 usage 及指定测试文本。赠送资源包的实际抵扣归属仍需通过账单或资源包余额核对。后续可将智谱 API 作为公开车型资料抽取与初步结构化工具，但不得作为事实来源。
- 智谱 API Key 不得读取、展示、写入聊天、文档、代码提交或长期记忆正文；后续脚本或流程只能引用环境变量 `ZHIPU_API_KEY`、`ZHIPU_MODEL`、`ZHIPU_BASE_URL`。智谱首批同样只处理公开车型资料，不传用户隐私、二手图片、账号信息、聊天原文或任何敏感数据。
- 成本路由原则：公开网页文本落盘后，优先用本机智谱 API 承担正文清洗、字段抽取、结构化 JSON、长文摘要、初步分切和批量格式转换；Codex 只负责来源获取/可访问性、规则校验、冲突识别、抽样复核与最终验收。调用尽量采用批处理、固定 JSON Schema、短提示词和低强度思考，并记录每批 usage、失败重试次数和输出文件。模型输出始终是 `extraction_draft`，不得自动升级为 `verified` 或 `recommendation_ready`。
- UHR150 单车路由验收表明：`glm-4.5-air` 关闭思考后三次均可返回可解析 JSON，三次 total token 合计 4358，显著低于 `glm-5.3` 两次失败调用的 11252；但它对“同车两个配置必须拆分为两条记录”的执行不稳定，首验 28/28 字段完整，相同参数复测却只返回 1 条合并配置（14/28）。因此 `glm-4.5-air` 当前尚未通过批量化路由验收，需先拆分配置输入或增加确定性 Schema/配置数量校验。
- 后续最小修正验收已通过：将 UHR150 扶手版和气囊减振/尾箱版拆为独立输入，`glm-4.5-air` 每次只返回一个 `model` 对象，两配置首次抽取及扶手版重复抽取均为 HTTP 200、JSON 可解析、14/14 字段完整、白名单来源通过且未发现幻觉；三次 total token 合计 3241。本地合并正确绑定 `2026/扶手版/13780/140kg` 与 `2026/气囊减振/尾箱版/14680/144kg`，复测稳定。因此 `glm-4.5-air` 可用于“调用前按年款+具体配置拆分、单次单对象、本地确定性合并和失败阻断”的批量 `extraction_draft` 路由，不得恢复多配置同批抽取。
- Ethan 反馈其账户页面显示 `glm-4.5-air` 约有 1000 万可用额度；该信息尚未独立核对适用范围和抵扣明细，不得写成平台已确认事实。后续取消固定 15000 tokens 阶段上限，但保留单请求 `max_tokens`、失败最多重试 2 次、逐批 usage、JSON/Schema/URL 白名单/配置绑定校验，以及连续 3 次解析或结构失败熔断；不得设置无限循环。
- SR250GT、五羊本田 PCX160、雅马哈 NMAX155 小批量预检均发现当前中国官网展示、页面价格位于 1 万至 3 万元且摩托范可交叉核验。本田系记录必须区分五羊本田与新大洲本田，本轮仅采五羊本田 PCX 标准版。雅马哈 NMAX Tech Max 与 25周年纪念版均有独立中国官网页面和官方建议零售价；具体 2026 年款仍依赖平台证据。SR250GT 官网价格未明确指导价口径，ABS/TCS 缺官方文字，PCX 功率和 HSTC 细分状态待复核。
- 本轮 `glm-4.5-air` 共调用 7 次，全部 HTTP 200 且 JSON 可解析，实际合计 8733 tokens。PCX 标准版及 NMAX 两配置通过 Schema、白名单和配置绑定；SR250GT Play/Pro 首次及各一次重试均把配置名并入 `model_search_name` 且令 `trim_name=null`，因此最终 5 配置路由通过率为 60%，`batch_route_approved=false`，未触发三连结构失败熔断。模型产物均保留为 `extraction_draft`，本轮没有配置完成完整 `recommendation_ready` 准入。
- Ethan 反馈其账户页面显示 `glm-4.7` 约有 500 万 Token；该信息尚未独立核对适用范围及抵扣明细，不得写成平台已确认事实。额度不作为默认模型选择的主要依据，仍以结构正确率、事实约束、稳定性、延迟和 Token 效率决定。
- 使用 SR250GT Play、SR250GT Pro、UHR150 扶手版完成 `glm-4.5-air`、`glm-4.6v`、`glm-4.7` 同输入纯文本 A/B/C。首次 9 次均 HTTP 200、JSON 可解析、URL 白名单通过且未发现无来源字段补全。`glm-4.5-air` 通过 2/3，SR Pro 为 13/14 且 `trim_name=null`，唯一重试仍失败；`glm-4.6v` 与 `glm-4.7` 均通过 3/3。首次三样本合计：4.5-air 3631 tokens/平均 25.61 秒，4.6v 3723 tokens/平均 22.81 秒，4.7 3367 tokens/平均 11.81 秒。当前建议把 `glm-4.7` 作为公开文本单配置抽取默认候选，`glm-4.6v` 作为图片/截图抽取暂定候选但必须另做视觉基准；未永久修改 `.env` 或默认模型。文本失败降级顺序建议为 4.7 → 4.6v → 4.5-air → 人工复核。
- P0五检索单元扩建中，450CLC被确认为450CL-C的无连字符别名，不创建重复车型；450CL-C按标准版、AMT版、单座版拆分。灰石250按手动与CVT拆分。CB190R属于五羊本田，CBF190R属于新大洲本田体系，不得合并。赛250燃擎版因官网当前目录缺失与平台在产冲突而排除；CBF190R因原官方页下架且当前目录未找到而排除。
- 本批新增9个单配置证据/草稿记录，其中3个记录拥有较完整的官方字段证据，但该描述不表示整车已核验、可进入规则池或可推荐；其余6个存在字段缺失/价格冲突，另有2个排除。450CL-C单座版官网同页出现23980与24380元冲突；灰石250与赛250MINI只有平台指导价、官方价格未闭环；CB190R官网与平台配置/价格存在冲突。这些记录不得用平台价冒充官方裸车价。
- `glm-4.7` 本批首次9次均HTTP 200且JSON可解析，首次门禁7/9通过、10012 tokens、平均约13.01秒；4次有限重试后实际13次调用合计14318 tokens。450CL-C AMT和灰石250 CVT首次及两次重试均`trim_name=null`，重试通过率0/2，最终批次仍为7/9，未触发连续结构失败熔断，未发现白名单外或无来源补全。结合此前A/B/C 3/3，`glm-4.7`当前滚动单配置首次通过率为10/12（83.3%）；继续作为默认文本抽取模型，但必须保留配置绑定门禁。
- 当前知识库工作约定升级为`kb_status_v0.2 / kb_admission_v0.2`：整车级`field_verified`、`pending_verification`和`recommendation_ready`混合语义停止作为当前状态依据，改用三个正交维度：`fact_verification_status`只描述字段事实证据，`rule_pool_eligibility`只描述能否参与确定性筛选，`recommendation_review_status`只描述推荐层复核。`eligible`不等于可推荐；只有规则池`eligible`且推荐复核`approved`才能进入最终推荐池。
- V0.2最小可信准入约定：当前在售与价格必须有官方证据；官网无明确年份时使用`current_version`，不得用平台年份伪造官方年款；官网“售价”保存为`official_public_price_cny`，不得称指导价。排量、座高、车重、功率、油箱、ABS/TCS官方优先，官方缺失时需两个独立主流平台一致且无冲突才可标`provisional_verified`；在售和价格不得降级为provisional。
- V0.2低人工复核约定：确定性脚本覆盖全量；Codex复核所有异常和所有准备进入最终推荐池的记录，必须称“Codex复核”而非人工核验；Ethan只审核政策变更、官方价格/在售冲突和每批10%确定性随机样本。旧`knowledge_base_outputs/verified/`文件全部保留，当前语义通过`legacy_manifest`映射到`verified_fields/eligible_pool/exceptions`，不依据旧目录名推断状态。
- 正式推荐批准责任机制已确认：拟发布批次必须先通过确定性门禁全量检查，再由Codex复核该批全部拟发布记录；Ethan完成10%确定性随机抽样，并在无未解决政策变更、官方价格或在售冲突后按批次批准发布，无需逐车逐字段批准。该批准是产品发布决策，不代表Ethan对全部车型事实进行了人工核验；未完成批次批准不得标记`recommendation_review_status=approved`。
- 正式推荐批准记录Schema已落地为`knowledge_base_outputs/recommendation_approvals/recommendation_approval_batch_v0.1.schema.json`，记录发布范围与版本、全量门禁报告、Codex复核、10%确定性随机抽样、未解决阻塞项和产品负责人决定。只有门禁通过、两层复核完成且阻塞项为空时才允许`approved`；当前尚未创建真实批准批次，第一批8配置的`recommendation_approved_count`仍为0。
- Ethan对CB190R价格处理明确选择方案B：五羊本田官网公开价格写入`official_public_price_cny`并作为正式价格依据；平台价格仅写入`platform_reference_price_cny`和备注，官网与平台价差本身不再标记`price_conflict`。标准版与平台运动版映射不明、官网未显式年款及多数参数证据不足等blocker继续保留，因此CB190R标准版和GP版仍为`rule_pool_eligibility=blocked`，不得自动进入eligible或推荐approved。
- 状态重构批次的2条10%样本已完成Codex运营复核，但Ethan业务抽样尚未执行；不得称为人工复核或Ethan已抽查。当前四行摘要为：抽查数2、错误放行数0、可疑数据数2、需Ethan决策数1。
- Ethan确认价格策略A：官网公开价格保留为`official_public_price_cny`；平台价只有同时满足同年款/同一`current_version`、同配置、同口径且24小时内核验时，才写入`qualified_platform_reference_price_cny`。预算准入使用`budget_guard_price_cny=max(official_public_price_cny, qualified_platform_reference_price_cny)`；没有合格平台价时使用官网公开价。较低平台价只作为“可能优惠/市场参考”展示，必须标平台、核验时间、非成交价和门店确认提示；配置或口径无法匹配时不得参与预算筛选。
- 官网价与平台价差异不再一律视为事实冲突，先分类为`explainable_market_difference`、`configuration_or_scope_unclear`或`true_same_scope_conflict`；只有真正同年款/当前版本、同配置、同口径冲突才阻塞整车。CB190R两配置的平台价因年款/配置无法匹配，分类为`configuration_or_scope_unclear`，合格平台参考价为null，预算保护价分别为标准版12980元、GP版13980元；其非价格blocker和blocked状态不变。
- Ethan已确认价格按需刷新产品/技术策略，但实现尚未开始：用户询价采用`cache-first / stale-while-revalidate`。缓存有效直接返回；缓存过期时展示旧值与最后更新时间并后台刷新；无缓存才显示核验进度。刷新目标20秒，30秒强制超时；失败或超时保留旧值并标`stale`，禁止用模型记忆、语言模型推断或无来源数值补价。
- 价格刷新来源优先官方公开API和官方页面，主流平台只作补充；官网价与平台价分开记录、互不覆盖。新价格必须匹配车型、当前版本/明确年款、具体配置和价格口径，通过确定性校验后才追加价格历史并重新计算`budget_guard_price_cny`。每条历史至少保存`source_url`、`source_name`、`captured_at`、`valid_until`、`configuration_match`、`verification_status`、价格口径、车型/版本/配置标识和刷新结果。
- 价格抓取安全策略已确认但尚未实现：网页内容视为不可信输入，必须使用来源白名单、网页正文与系统提示隔离、提示注入隔离、域名级限流、超时和失败熔断，并遵守robots、服务条款及站点访问规则。抓取或校验失败不覆盖已有价格历史。
- 第一批 8 个配置已完成 `kb_status_v0.2 / kb_admission_v0.2` 端到端收口：UHR150 两配置、NMAX155 两配置、450NK 标准版、450CL-C 标准版/AMT版、PCX160 标准版。8 条全部为终态 `rule_pool_eligibility=eligible`，`blocked=0`，`pending_deterministic_check=0`，错误放行 0。
- 第一批全部 8 条及异常已完成 Codex operational review，状态为 `recommendation_review_status=codex_reviewed`；该复核不是 Ethan 人工/业务复核。本批 `recommendation approved=0`，不得伪造为最终推荐获批。
- 第一批规则池可用于 MVP 在售、预算等确定性规则筛选。PCX160 未满足两独立平台一致的功率已置 `null`，HSTC 细分不参与硬规则；NMAX155 车型类型按非硬字段状态化；450CL-C AMT 配置名从已核验官方单配置证据确定性重建，未使用失败的模型输出。
- 第一批 8 个配置的独立语义增强层 `first_batch_semantic_v0.1` 已建立并通过确定性校验，未覆盖正式规则池，未改变 recommendation review/approval，也未调用外部模型。
- 语义 V0.1 中，8 条用途标签和风格标签、7 条已知动力档、7 条可测参数操作压力及新手友好候选值可作为版本化内部排序输入。操作压力仅使用功率、车重和座高，油门响应、低速操控和经验要求仍是未知；新手友好是待用户测试校准的产品假设，不是安全保证。
- PCX160 因功率仍为 `null`，其动力档、操作压力和新手友好保持 `unknown`。第一批 8 条均缺少可核验的服务网络、配件供应和维护成本证据，因此 `maintenance_convenience` 全部为 `unknown`且不参与排序；未达“1个专业评测+3条独立车主讨论”门槛，未生成口碑结论。
- Ethan 已于 2026-08-29 对语义 V0.1 选择产品策略方案 A：允许现有 `usage_tags`、已知 `operation_pressure_level`、`beginner_friendliness`、`style_tag`和已知 `power_band` 以低权重进入内部候选排序，用于首轮用户测试校准。该决策是产品策略批准，不是对 8 条数据的逐字段事实人工核验，不构成 recommendation approval。
- 语义 V0.1 排序使用边界：不对外展示产品假设的高/低分，不将其表述为客观骑行结论或安全保证。`maintenance_convenience` 全部保持 `unknown`且不参与排序；PCX160 缺失的功率、操作压力、新手友好和动力档作为中性缺失，不插补、不加分也不扣分。本批 `recommendation_approved_count` 仍为 0。

## 2026-08-31

- 本机 GitHub CLI 链路已打通：Apple Silicon 版本 `gh 2.98.0` 安装在用户级路径 `~/.local/bin/gh`，安装归档已按 GitHub 官方 SHA-256 校验；账号 `moronfranklyn-lab` 已通过 GitHub 官方设备流程认证，令牌保存在 macOS 钥匙串，权限范围为 `gist/read:org/repo/workflow`。当前仓库远端为 `https://github.com/moronfranklyn-lab/motomate.git`，GitHub API 核验该私有仓库对当前账号开放 `admin/maintain/push/pull/triage` 权限，可在 Codex 中查看和管理 Pull Request；2026-08-31 验证时开放 PR 列表为空。长期记忆不得保存 OAuth 令牌、设备码或其他认证秘密。
- 无极 CU250Ⅱ代手动挡、自动挡已完成小批次核验。无极官网前端公开目录API分别绑定详情id 31和37；官网详情API公开价格为16480/18980元，参数为250mL、710mm、20kW、15L，整备质量分别178/182kg，变速器分别国际6挡/AMT6速自动挡。
- CU250Ⅱ代两配置进入独立`mvp_eligible_pool_v0.1`，MVP eligible由15条增至17条；原Alpha 15条测试池保持不变，避免未经接入评估直接扩大前端和现有规则引擎范围。35条候选Registry现为`eligible=17`、`blocked=9`、`excluded=6`、`ineligible=3`，`recommendation_approved_count=0`。
- 无极官网详情仅描述制动结构，未明确写ABS/TCS；汽车之家单平台展示ABS不足以达到两平台一致的`provisional_verified`门槛，因此CU250Ⅱ代两配置ABS/TCS均保持null。摩托范页面本轮受WAF阻断，不作为新增字段证据。
- CU250Ⅱ代官网API的`price`按`official_public_price_cny`保存，标签为“官网公开API price字段”，不改写成指导价；汽车之家数值只作平台参考且不参与预算保护价。本轮未调用智谱，Token使用0。

- CGX150 标准版、边包版、特别版已完成小批次收口。五羊本田官网当前车型页明确展示三个配置、建议零售价及主要参数；汽车之家正确车系页 `series/10211222` 明确列出2025款三个同名配置及对应参考价。原草案 `series/10210785` 实际指向布沃克VMX，已纠正且不得再作为CGX150来源。
- CGX150 三配置已进入内部 Alpha 规则池，使 Alpha 从12条增至15条，35条候选 Registry 状态更新为 `eligible=15`、`blocked=11`、`excluded=6`、`ineligible=3`。三条均为 Codex operational review，不是Ethan逐字段人工核验；`recommendation_approved_count=0`。
- CGX150 官网建议零售价分别为标准版10080元、边包版11480元、特别版11680元；汽车之家2025款同配置参考价数值一致，但官网只声明`current_version`，因此平台值仅保存为`platform_reference_price_cny`，不标`qualified_platform_reference_price_cny`，预算保护价仍使用官网公开价。
- CGX150 官网核验参数为149mL、740mm、8.8kW、10L、前轮ABS；整备质量按标准/边包/特别版分别为125/128/126kg。TCS无明确证据，保持null。本次状态升级基于来源复核和确定性规则，没有新增智谱调用，Token使用为0。

- Ethan 选择方案 A，允许在既有品牌与 1 万至 3 万元裸车公开价格范围内建设 25 至 35 个 MVP 单配置候选；候选 Registry 可包含 `eligible/blocked/ineligible/excluded`，但不得为凑数降低 `kb_admission_v0.2` 门槛。
- `mvp_candidates_v0.1` 已形成 35 个候选检索单元，状态为 `eligible=12`、`blocked=14`、`excluded=6`、`ineligible=3`。12 条 eligible 仅来自已验收的 Alpha 池；新增候选未自动升级，`recommendation_approved_count=0`。
- 本轮新增 11 份公开证据单配置输入，并使用 `glm-4.7` 完成 extraction draft：11/11 首次 HTTP 200、JSON 可解析且单对象结构通过，无重试、无熔断；prompt 4,801、completion 4,481、total 9,282 tokens。确定性检查未发现白名单外 URL 或非空字段无来源。模型输出仍不是事实来源。
- 新增候选状态：无极 CU250Ⅱ代手动/自动因缺可复现官网公开价格及参数交叉证据保持 blocked；无极 300AC、300DS 标准/三箱因当前官网目录未找到而 excluded；升仕 350D/350E/350GK 因官网已下架而 ineligible；五羊本田 CGX150 标准/边包/特别版虽有较完整官网证据，但在独立平台同配置交叉核对完成前保持 blocked。

- Ethan 选择知识库 Alpha 扩建策略 A（覆盖优先）。在不降低 `kb_admission_v0.2` 门槛的前提下，新增豪爵 TR300 都市版、TR300 旅行版、XCR300 标准机械配置和五羊本田 CB190SS 辐条轮版 4 条 `eligible`记录，形成 `alpha_pool_v0.1` 共 12 条。原第一批 8 条保留不覆盖。
- Alpha 12 条仅限内部 `development_preview`；新增 4 条均为 `recommendation_review_status=codex_reviewed`，该复核不是人工事实核验。整池 `recommendation_approved_count=0`，不得对外宣称正式推荐。
- Alpha 新增记录严格保留非硬字段缺失：TR300/XCR300 的 TCS 为 `null`；CB190SS 的座高、ABS、TCS 为 `null`，因此其操作压力和新手友好也保持中性未知。未用模型常识插补。
- 覆盖优先审计中，无极 SR250GT、奔达灰石250、QJMOTOR赛250MINI、CB190R、无极CU250等仍因官方价、主体、配置映射或参数冲突保持 blocked；升仕350D/350E/350GK官网明确已下架，记为 ineligible。不得为凑数放行或把后继车型状态迁移给旧车型。
- `alpha_semantic_v0.2` 已覆盖 12 条：用途/风格 12 条、操作压力/新手友好 10 条、已知动力档 11 条，维护便利性仍为 0 条可排序。语义层继续执行 Ethan 已批准的低权重内部测试策略，不对外展示分数。
- Alpha 池、语义层、原 8 条池及脚本语法校验全部通过；规则引擎现有 7 场景全通过，20 条工程冒烟为 20 passed/0 failed/0 blocked。本轮未调用智谱或豆包，模型 API 调用和 Token 均为 0。
- 20条工程冒烟契约已于2026-08-31落地到`evaluations/smoke/smoke_cases_v0.1.json`，运行器为`scripts/run-smoke-evals.mjs`。当前真实基线是5条规则引擎案例通过、0条失败、15条因对应模块尚未实现而`blocked`；阻塞模块包括预算换算、充分度判断、会话状态、推荐管线、外部候选核验、价格解析、结果校验、二手文字风险工具和费用门禁。`--require-all`在存在blocked时返回非零状态，防止把测试契约存在误写成20条功能已通过。
- 内部Alpha第一段决策核心已于2026-08-31落地为`decision-core.js`：支持裸车/购车总预算口径、默认费用区间换算、推荐最低字段充分度、每轮单问题和最多3次关键追问。对应`smoke_003`至`smoke_008`已从blocked转为真实通过；当前20条冒烟基线更新为11条通过、0条失败、9条blocked。模块只处理确定性结构化输入和简单预算金额识别，不代表自然语言需求提取模型、完整编排器或页面链路已实现。
- 内部Alpha推荐链路第一段已于2026-08-31继续落地为`recommendation-pipeline.js`和`result-validator.js`：支持规则池无完全匹配时分区输出最接近候选、逐项标出未满足条件、禁止自动放宽条件，并拦截候选池外车型输出。对应`smoke_012`和`smoke_016`已从blocked转为真实通过；当前20条冒烟基线更新为13条通过、0条失败、7条blocked。该阶段仍只提供本地确定性编排和模板化解释骨架，不代表自然语言需求提取、价格刷新、二手文字助手、费用门禁或外部正式推荐批准已实现。
- 内部Alpha价格解析层已于2026-08-31落地为`price-resolver.js`：支持有效缓存直接展示、过期缓存保留旧值并显示最后核验时间及刷新状态、无缓存时进入核验等待、禁止模型记忆补价；`true_same_scope_conflict`候选不得成为首选，只能进入独立待核价候选区。独立测试6个场景通过，`smoke_014`和`smoke_015`已从blocked转为真实通过，当前20条冒烟基线更新为15条通过、0条失败、5条blocked；`--require-all`因剩余阻塞按预期返回2。该实现仅为本地确定性决策层，真实联网刷新、来源白名单、限流、超时、熔断和价格历史写入仍未实现。
- 轻量二手文字风险模块已于2026-08-31落地为`used-text-risk-tool.js`：仅整理用户提供的结构化车源字段，输出信息缺口、待确认风险、卖家问题、线下验车清单和不替代专业检测声明；字段缺失时不推断车况，请求判断事故、泡水、调表或真实车况时拒绝下诊断结论并转为线下核查指引。独立测试4个场景通过，`smoke_017`至`smoke_019`已从blocked转为真实通过，当前20条冒烟基线更新为18条通过、0条失败、2条blocked；剩余模块为`external_candidate_verifier`和`cost_guard`。该实现不包含自然语言字段抽取模型、图片分析、二手行情判断、车辆鉴定或交易撮合。
- 首轮模型费用门禁已于2026-08-31落地为`cost-guard.js`：默认50元硬上限，累计费用达到上限或预计下一次付费调用会越过上限时，禁止新付费模型请求并进入基础模式；规则筛选、固定模板和历史查看继续可用，成本数据缺失或无效时保守阻断付费调用。独立测试5个场景通过，`smoke_020`已从blocked转为真实通过，当前20条冒烟基线更新为19条通过、0条失败、1条blocked；唯一剩余模块为`external_candidate_verifier`。该实现是本地确定性门禁，真实usage计费换算、持久化累计、并发扣费原子性、管理员告警和请求网关接入仍未实现。
- 范围外临时候选证据门禁已于2026-08-31落地为`external-candidate-verifier.js`：品牌官网必须通过车型身份、当前在售和关键参数核验，至少一个主流平台价格证据必须通过同配置/同口径匹配，真正同口径价格冲突必须阻断；证据不足时只输出选车原则，通过时也仅标记为临时候选，不自动成为正式推荐。独立测试5个场景通过，`smoke_013`已从blocked转为真实通过；当前20条工程冒烟与`--require-all`均为20条通过、0条失败、0条blocked。该结果只证明本地工程契约闭环，不代表真实联网检索、网页抓取、来源白名单、安全隔离或生产推荐批准已完成；第一批8配置的`recommendation_approved_count`仍为0。
- 内部Alpha统一编排入口已于2026-08-31落地为`alpha-orchestrator.js`：每次请求先执行费用门禁，再按主意图路由到新手推荐、价格状态、二手文字风险、范围外候选或非购车重定向；新手推荐先经过最低信息充分度和单问题追问，再调用规则池、语义层、推荐管线与结果校验。8个端到端场景通过，既有20条工程冒烟仍全部通过。编排器强制标记`development_preview`且`formal_recommendation_allowed=false`，费用达到50元时仍只使用规则和固定模板。当前自然语言提取仅为可审计的金额/用途/新旧/车型关键词识别，不代表完整Need Extractor；后端API、模型解释、会话状态持久化、反馈闭环、审计日志和真实联网工具仍未实现。
- 内部Alpha本地API已于2026-08-31落地为`server/alpha-api.js`：提供`GET /health`、`POST /api/alpha/run`和`GET /api/sessions/:id/audit`，支持多轮结构化需求合并、删除字段覆盖、关键追问计数和脱敏运行事件；审计事件不保存用户原始文本。API初始独立测试5个场景通过，既有编排器与20条工程冒烟继续通过。该初始版使用Node内置HTTP与进程内存会话，服务重启会清空状态；这是已被后续SQLite持久化节点取代的历史状态。
- 内部Alpha网页与API已于2026-08-31完成首次真实接入：`server/alpha-api.js`同时托管静态页面和编排接口，网页删除虚构车型目录及浏览器本地推荐算法，只渲染后端候选池、二手文字核查和失败降级结果。API已显式切换到独立`alpha_pool_v0.1`与`alpha_semantic_v0.2`共12配置；原`first_batch_v1`继续作为8配置正式边界池供既有生产门禁测试使用。12配置全部仅限`development_preview`且`recommendation_approved_count=0`。桌面选车、二手入口和390px移动端已通过浏览器验证，无文档横向溢出；API测试更新为6个场景。该接入仍不包含正式模型解释、持久化、认证或公网发布。
- 内部Alpha反馈闭环与推荐版本已于2026-08-31接入API和网页：每次生成候选创建不可覆盖的`recommendation_version`，保存需求快照、Alpha数据池版本、规则版本、候选model_id和生成时间；用户后续修改需求产生新版本。反馈使用1至5分帮助度、至少一个具体帮助标签和可选说明，1至3分必须选择失败原因；只有评分不低于4且存在具体帮助标签才标记`success_sample=true`。反馈只能对同一版本提交一次，审计记录不保存原始咨询文本。API自动化在该节点更新为10个场景，浏览器已验证V1有效帮助提交、V2版本递增及低分缺原因拦截。该节点的版本与反馈仍存于进程内存；这是已被后续SQLite持久化节点取代的历史状态。
- 内部Alpha本地持久化已于2026-08-31接入`server/sqlite-store.js`：使用本机`sqlite3`将数据分为`sessions`、`recommendation_versions`、`feedback`和`audit_events`四表，推荐与反馈写入均使用事务；数据库默认位于`runtime/motomate-alpha.sqlite`且不进入Git。存储层仅保存结构化需求、候选ID、反馈和脱敏审计事件，不保存用户原始咨询文本。API自动化共12个场景通过，包含关闭服务、用同一数据库重启、再读取推荐版本与反馈审计数据。这仅是单机内部Alpha持久化；正式多人上线前仍需要认证、云数据库、备份恢复、用户删除与数据保留周期、并发治理和公网安全配置。
- 内部Alpha的结构化“查看依据”链路已于2026-08-31接入规则引擎和网页：每个候选输出入选原因、官方公开价、预算保护价及其取值策略、价格/座高/车重/功率/ABS/TCS核验状态、公开来源、规则评估时间和Codex运营复核时间。网页使用可折叠面板按需展示，不展示内部语义分数，不把`codex_reviewed`表述为专家或正式批准。桌面端真实提交与展开已验证，控制台无错误；390px移动端`scrollWidth=390`，无横向溢出。本阶段复用现有知识库来源，未调用网页爬取API；真实价格过期刷新和范围外候选获证仍是后续联网工具阶段。
- 内部Alpha反馈复盘链路已于2026-08-31接入`server/sqlite-store.js`、`GET /api/internal/feedback-summary`和网页“反馈复盘”视图：汇总推荐版本数、反馈数、反馈覆盖率、平均评分、有效帮助数/比率、评分分布、具体帮助标签、低分失败原因及最近20条反馈。复盘数据不返回`session_id`，不新增用户原始咨询文本采集。API自动化更新为13个场景；真实本地库已在桌面与390px移动端渲染通过，无横向溢出或控制台错误。反馈覆盖率与平均分必须同时解读，避免只看主动反馈者的幸存者偏差。当前复盘接口没有管理员认证，仅限本地内部Alpha，不得在公网部署中暴露。
- 首轮内部Alpha测试准备已于2026-08-31形成双门禁：A门是5人陪同式可用性测试，只验证需求表达、结果理解、依据可见性、需求修改和反馈完成；B门是10至20人外部邀请Alpha。已新增`testing/首轮内部Alpha测试执行手册.md`、脱敏观察CSV、五人汇总模板和`scripts/check-alpha-test-readiness.mjs`。实时门禁会真实运行20条工程冒烟与13个API场景，并检查风险提示、反馈复盘、存储边界和测试文件。当前结果是A门`ready=true`、B门`ready=false`；B门阻塞为正式推荐批准、50条黄金评测、认证、云数据库与备份、用户删除、邀请码与容量、每日限额、真实费用计量和管理接口保护。当前`recommendation_approved_count=0`，陪同测试必须逐字说明开发预览边界，不得用于真实付款决策。
- 2026-08-31陪同观察收到两项可用性反馈：新手不理解功率/座高/车重/ABS/TCS的决策意义，且原工作台式界面同时展示过多信息。Ethan确认主界面改为纯对话式Agent，不照搬参考产品品牌或内容。已完成展示层重构：主流只保留对话、快捷问题、候选、按需依据和内联反馈；内部复盘作为独立侧栏视图。候选首层改用“日常挪车、坐上去的感受、安全辅助”翻译参数，专业数值与证据继续放在可展开依据中。座高不以身高直接做确定性匹配，因为腿长、坐垫宽度、悬挂下沉和车辆重心会改变实际着地；改为要求到店确认单脚稳定着地与原地挪车。宽屏与390px移动端已通过截图和布局复验，修复了移动端用户消息被压成单字竖排的问题；工程冒烟20/20、API 13场景通过。
- 参考 Lollipopp 实际产品交互后，MotoMate 确认采用“自然语言理解意图、对话内轻控件确认、卡片呈现结果、独立视图沉淀复盘”的 Agent 结构，只借鉴渐进披露与任务闭环，不照搬岗位广场、积分或营销模块。首屏入口调整为“不知道买什么、两款车在纠结、核查二手车源”三个真实任务；快捷追问可直接点击作答。移除底部常驻的新车/都看/二手分段控件及默认新车状态，恢复“新旧偏好缺失必须追问”的既定规则。预算口径、预算、用途和新旧偏好齐全后，系统先在对话流展示需求确认卡，用户确认后才执行筛选，降低错误理解直接进入推荐的风险。桌面真实候选链路、390px布局与需求确认卡均通过浏览器验证，无横向溢出或控制台错误；工程冒烟20/20、API 13场景通过。Alpha权威清单为15配置，页面与README已由旧的12配置口径纠正为15；15配置仍全部仅限development_preview，recommendation_approved_count=0。
- Ethan 指出真实顾客不会主动要求“搜索库外车型”，只会要求推荐适合自己的车型。MotoMate 因此确认由编排器负责判断候选覆盖并自动决定是否需要联网扩展，不把内部知识库边界暴露为用户任务。推荐质量V0.2第一段已实现`candidate-coverage.js`：检查候选少于3个、预算上沿未覆盖、预算层次不足及未指定车型时的类型单一问题，覆盖不足时生成结构化`search_request`与`external_search_required=true`。预算仍是上限而非消费目标；展示层从符合硬规则和用途信号的候选中优先选择“省心入门、均衡选择、预算内升级”，不再同分时直接截取最低价前三名。3万元通勤踏板真实链路已展示13780元UHR150、19990元PCX160、27800元NMAX155三个层次；专项8场景、规则7场景、编排8场景、工程冒烟20/20、API 13场景通过，浏览器控制台无错误。真实网页搜索、抓取安全和自动证据获取仍未接入，当前不得声称已实现联网推荐。
- Ethan 提供博查 Web Search 作为联网搜索能力。真实密钥只保存在本机`.env`的`BOCHA_API_KEY`，不得写入聊天、代码、文档、日志、审计事件或长期记忆；此前曾粘贴到聊天的旧密钥已要求撤销轮换，项目不记录其内容。官方接口核验为`POST https://api.bochaai.com/v1/web-search`、Bearer鉴权，支持query、freshness、summary和count；返回标题、URL、站点名、摘要及发布时间等。已实现`bocha-search-client.js`，默认8秒超时、最多10条、HTTP(S)和回环地址防护、文本截断、摩托车相关性过滤及按实际URL域名分类来源。第三方`siteName`曾将`m.58moto.com`错误标成技术文档站，因此来源身份必须按URL域名判断，不能相信返回标签。3次真实连通/质量调试调用均成功返回，搜索结果仍统一标记`evidence_verified=false`、`candidate_use_allowed=false`，不会直接进入候选。
- 博查自动搜索已接入候选覆盖不足的本地Alpha链路。1.8万元新车巡航通勤真实页面场景在本地无完全匹配时，自动发现8条相关来源线索，同时仅把450CL-C、TR300等超预算本地车型分区显示为“最接近”，未自动放宽预算，也未将联网线索伪装为候选。博查客户端13个离线场景、自动搜索集成1场景、工程冒烟20/20、API 13场景通过，真实页面控制台无错误。当前完成的是“候选发现”，不是“证据核验和库外推荐”；下一阶段需从来源线索中提取车型身份，分别获取官方在售/参数证据和同配置平台价格证据，通过`external-candidate-verifier`后才可成为临时候选。
- 库外临时候选证据管线已于2026-08-31完成代码接入与首次真实网页失败试跑：`external-candidate-discovery.js`负责确定性提取品牌、车型、年款、价格和类型并与35条候选Registry去重；`web-evidence-fetcher.js`限制官网/平台域名、DNS私网地址、重定向次数、最大1MB页面并检测提示注入；`external-evidence-pipeline.js`要求品牌官网车型身份/在售/关键参数与主流平台同配置/同口径价格双源同时通过，最终仍只允许`verified_temporary_candidate`。1.8万元巡航场景真实试跑找到春风250CL-C摩托范价格页，但未找到允许域名内的官网车型证据，结果为`external_verification_required`、`candidate_released=false`，证明缺证据会安全阻断。该条记录描述正向路径完成前的历史节点，最新状态以下一条为准。
- 库外临时候选正向路径已于2026-08-31完成真实验收。搜索先做通用发现；没有可用草稿时，按车型类型最多执行2次品牌定向补搜。用户指定车型类型时，类型未知的草稿不得进入证据核验。官网可在同域名、有限页面数内组合车型概览、配置价格和参数证据；泛车型只有在官网文本形成明确“价格紧邻配置名”配对时，才按最低价入口配置收敛，反向顺序因存在跨配置误配风险而禁止。豪爵TVL350泛车型由官网确定性收敛为扶手版，官网与摩托范同年款/同配置价格证据通过，`budget_guard_price_cny=28980`，真实脚本及自然语言页面均显示`verified_temporary_candidate`；页面同时明确其不是正式推荐，座高未知时不做身高适配推断。核验后另有预算二次门禁，价格超过用户预算时撤销临时候选。当前博查17场景、证据管线30场景、自动搜索集成9场景、编排器8场景、Alpha API 13场景、工程冒烟20/20通过。4万元踏板场景只用于触发预算覆盖不足的技术验收，不表示MVP正式1至3万元范围扩大；`recommendation_approved_count=0`不变。
- 50条离线产品黄金评测已于2026-08-31落地为`evaluations/golden/golden_cases_v0.1.json`与`scripts/run-golden-evals.mjs`，绑定17配置`mvp_eligible_pool_v0.1`和语义层`mvp_semantic_v0.1`。覆盖conversation 6、intent 3、recommendation 19、coverage 3、budget 3、price 4、used 4、external 6、cost 2，首次运行50/50通过。黄金集建设发现落地总预算虽完成费用预留计算、推荐管线却仍使用原始总预算的缺陷；现已将原始预算保留为需求展示，并将`filter_budget_cny`传入规则筛选、预算分层与最近候选计算。3万元落地预算当前按26,500元可用裸车上限筛选，不再包含27,800元NMAX155；3万元裸车预算仍正常包含。编排器测试更新为11场景，既有候选覆盖、规则、API和冒烟回归通过。黄金评测已接入外部邀请Alpha实时门禁，但50/50只代表离线产品契约成立，不代表正式推荐批准或公网发布就绪；`recommendation_approved_count=0`不变。
