# GLM-4.5-Air UHR150 模型路由验收报告

验收时间：2026-08-28（Asia/Shanghai）。

## 验收结论

`glm-4.5-air` 可在关闭思考后返回非空、可解析 JSON，成本明显低于 `glm-5.3`。但相同输入与参数的稳定性复测未稳定拆分两个 UHR150 配置，所以本轮结论为：

- 文本/JSON 返回能力：通过。
- 来源约束：通过，未发现白名单 URL 之外的来源或输入证据之外的数值补全。
- 固定 Schema 与配置唯一性：不稳定。
- 批量化路由：暂不通过。

## 请求参数

- 模型：`glm-4.5-air`
- 模型选择：`ZHIPU_MODEL`
- 思考：`thinking.type=disabled`
- `max_tokens=1600`，可由 `ZHIPU_MAX_TOKENS` 覆盖
- `temperature=0.1`
- 输入：`knowledge_base_inputs/public_sources/uhr150_2026_sources.md`
- API Key：仅由本机 `.env` 加载，未写入请求日志或产物

## 调用结果

| 轮次 | HTTP | JSON | 结构/配置 | 字段完整率 | 无来源补全 | prompt | completion | total | 输出 |
|---|---:|---|---|---:|---|---:|---:|---:|---|
| 预检 | 200 | 可解析 | 失败：根键为 `answer`，两配置合并 | 50% | 未发现 | 759 | 483 | 1242 | `zhipu_2026-08-28T06-15-15-972Z.json` |
| 收紧 Schema 首验 | 200 | 可解析 | 通过：`models[]` 两配置 | 100% (28/28) | 未发现 | 773 | 1032 | 1805 | `zhipu_2026-08-28T06-16-42-456Z.json` |
| 同参数稳定性复测 | 200 | 可解析 | 失败：只返回 1 条合并配置 | 50% (14/28) | 未发现 | 773 | 538 | 1311 | `zhipu_2026-08-28T06-17-33-430Z.json` |

完整率口径：预期 2 个配置，每配置 14 个目标事实字段，共 28 个；配置被合并时，即使单条字段非空，也只计 14/28。

## 与 GLM-5.3 对比

| 模型 | 调用数 | prompt 合计 | completion 合计 | total 合计 | 可解析 JSON | 稳定通过 |
|---|---:|---:|---:|---:|---:|---|
| `glm-5.3` | 2 | 2252 | 9000 | 11252 | 0/2 | 否 |
| `glm-4.5-air` | 3 | 2305 | 2053 | 4358 | 3/3 | 否 |

`glm-4.5-air` 三次调用总 Token 约为 `glm-5.3` 两次失败调用的 38.7%，且能返回可解析 JSON；但配置拆分不稳定，成本优势不能替代结构正确性验收。

## 下一步限定

暂不新增车型或启动批量采集。下一轮应先选择一种结构稳定化方案再验收：将不同配置拆成独立输入单元，或在抽取后增加确定性配置拆分与 Schema 校验；校验失败时必须阻断，不得自动升级。
