# 第一批 8 配置端到端收口报告

## 结论

- 范围：UHR150 两配置、NMAX155 两配置、450NK 标准版、450CL-C 标准版/AMT版、PCX160 标准版，共 8 条。
- 确定性规则池：`eligible=8`，`blocked=0`，`ineligible=0`，`excluded=0`。
- 全部 8 条已有终态，`pending_deterministic_check=0`。
- Codex 已复核全部记录和异常；这是 Codex operational review，不是 Ethan 人工/业务复核。
- 错误放行数为 0。`recommendation approved=0`，未伪造产品层批准。

## MVP 使用边界

这 8 条可用于 MVP 的在售、预算和其他确定性规则筛选。`eligible` 不等于最终推荐获批；在没有明确产品层授权前，不得将这一规则池对外表述为“已批准推荐车型”。

## 迁移与降级处理

- 全部记录的年份已规范为 `current_version`，旧年份观察仅作历史备注。
- 官网售价已迁移为 `official_public_price_cny`。本批平台价均不满足同版本、同配置、同口径及 24 小时核验的全部条件，因此 `qualified_platform_reference_price_cny=null`，`budget_guard_price_cny` 使用官网公开价。
- 450CL-C AMT 的 `trim_name` 已从官方单配置证据确定性重建，未使用失败的模型配置名输出。
- PCX160 功率只有单平台支持，已置 `null`并标为非硬字段未核验；HSTC 细分不参与硬规则。
- NMAX155 的车型类型标为 `platform_observed_non_hard`，不作为硬准入依据。

## 验收产物

- 正式规则池：`knowledge_base_outputs/eligible_pool/first_batch_v1.json`
- 活动版本清单：`knowledge_base_outputs/eligible_pool/manifest.json`
- 异常与非阻塞降级：`knowledge_base_outputs/exceptions/first_batch_closure_exceptions.json`
- 状态映射：`knowledge_base_outputs/exceptions/current_status_remap.json`
- 专用校验器：`scripts/validate-eligible-pool-v02.mjs`

## 剩余事项

规则池无阻塞项。仍缺的是后续产品层 recommendation approval，不是本批确定性规则池的准入证据。本报告完成后停止，不继续第二批。
