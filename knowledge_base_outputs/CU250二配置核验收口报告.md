# CU250Ⅱ代两配置核验收口报告

## 结论

无极 CU250Ⅱ代手动挡、自动挡已完成官网公开目录、官网详情 API 与汽车之家同配置交叉核验，达到 `kb_admission_v0.2`，进入 17 条 MVP eligible 池。内部 Alpha 仍保持 15 条测试上限，不自动扩大前端或规则引擎当前使用范围。

两条均为 Codex operational review，不是 Ethan 逐字段人工核验；`recommendation_approved_count=0`。

## 官方证据

- 无极官网前端调用的公开目录 API 将 CU250Ⅱ代和 CU250Ⅱ代自动挡列在“巡航太子系列”，详情 ID 分别为 31、37。
- `id=31` 官网详情：price 16480、250mL、710mm、178kg、20kW、15L、国际6挡。
- `id=37` 官网详情：price 18980、250mL、710mm、182kg、20kW、15L、AMT6速自动挡。
- 官网详情仅描述制动结构，没有明确写 ABS 或 TCS，因此两字段保持 `null`。

## 平台交叉

汽车之家参数页明确绑定 CU250Ⅱ代手动挡与自动挡，参考价和车重分别为 16480/178kg、18980/182kg，并交叉支持座高、功率和油箱。其 249.9cc 作为平台观察值，不覆盖官网 250mL。

摩托范车系页本轮受 WAF 阻断，记录 `blocked_by_waf`，不作为新增事实依据。汽车之家展示 ABS，但单平台不足以按 V0.2 升级为 `provisional_verified`。

## 数据边界

- 官网 API 的 `price` 保存为 `official_public_price_cny`，口径标记为“官网公开API price字段”，不擅自称指导价。
- 汽车之家价格只作平台参考；没有进入24小时合格平台价格历史流程，因此不参与 `budget_guard_price_cny`。
- 本轮没有调用智谱，Token 使用为 0。
